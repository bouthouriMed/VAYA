# VAYA

Production carpooling marketplace for Tunisia. Primary client: Expo/React Native mobile app.

## Stack

pnpm workspaces + Turborepo · Expo + React Native + Expo Router (TS strict) · Fastify + Drizzle ORM + PostgreSQL · Redux Toolkit + RTK Query · OpenAPI → generated client → RTK Query · Zod validation · Vitest (unit) + Playwright (E2E) · Docker Compose (Postgres + Redis)

## Layout

```
apps/{mobile,api,admin}
packages/{design-system,api-client,config,validation,domain,eslint-config}
tests/e2e
docker/            # compose for local Postgres + Redis + self-hosted OSRM (Tunisia routing)
docs/              # product/ux/design-system/domain/architecture/roadmap — see docs/product/README.md
```

## Commands

```
pnpm install && docker compose -f docker/docker-compose.yml up -d
pnpm dev:api        # :3000
pnpm dev:mobile      # Expo; add --lan for physical device over LAN
pnpm lint / typecheck / test / format:check
pnpm db:generate / db:migrate / db:studio
pnpm generate:api-client   # requires dev:api running
```

Mobile/API need `.env` (copy from `.env.example`). For Expo Go over LAN, set mobile's `API_BASE_URL` to the host's LAN IP, not `localhost`.

## Rules

- No God components/services, no business logic in UI, no direct HTTP calls from UI (RTK Query only), no direct DB access from controllers
- No circular deps, no premature abstraction/microservices/speculative features, no global mutable state
- No `any` without justification, no silent error swallowing, no secrets in git
- Never hand-edit `packages/api-client/src/generated/` — regenerate instead
- Mobile: feature dirs under `src/features/`; routes compose screens, logic lives in features
- API: Fastify HTTP layer, Zod validation, Drizzle for DB, errors via `AppError` hierarchy
- Reuse `@vaya/design-system` primitives instead of raw RN components

## Product vision

VAYA brings a structured, trustworthy, spatially-intelligent carpooling marketplace to Tunisia — a market with no incumbent international carpooling platform, dominated today by louages and informal social carpooling. VAYA's edge is two things it already does unusually well for its stage: a real driver-verification pipeline (live-camera KYC) and a real road-routing foundation (self-hosted OSRM over a Tunisia extract). The product's job is to build the two systems that are the actual hard part of a carpooling marketplace — route-aware stop selection and bounded pricing — on top of that foundation. Full detail: `docs/product/README.md`.

## Product principles

1. The marketplace's two hardest mechanics — where to stop, what to pay — must never be free-form entry. Always a small, validated, ranked set of choices.
2. Trust must be visible before commitment, not discovered after (ratings/tenure shown pre-booking, not post).
3. Build on what's real, don't rebuild what works — the matching algorithm (`matching.service.ts`), the OSRM routing foundation, the domain state machines (`packages/domain`), and the core design-system primitives are genuine assets.
4. Never let a screen show fabricated success — every field rendered after a mutation must come from that mutation's real response, or show an honest loading/error state.
5. Complexity is added when evidence justifies it, not preemptively (see NOW/NEXT/SCALE in `docs/architecture/overview.md`).

## UX principles

Full detail and anti-patterns: `docs/ux/principles.md`. Summary: spatial/map-first over administrative forms; progressive disclosure over dense screens; every loading/empty/error state is a designed product surface, not a default; before proposing a new screen ask whether the interaction can be simplified, combined, progressively disclosed, or made more contextual instead.

## Design-system rules

Full spec (actual token values, component inventory, gaps): `docs/design-system/README.md`. Hard rules:
- No raw React Native primitives (`View`/`Text`/`TextInput`/`TouchableOpacity`/`StyleSheet.create` with hardcoded values) in screens — use `@vaya/design-system`.
- If a screen needs a pattern the system doesn't have, build the primitive in `packages/design-system` first — don't improvise local styling that should have graduated into a reusable component.
- Consume tokens (`colors`, `spacing`, `radii`, `elevation`, `typography`) — never hand-roll equivalent values.
- VAYA's brand character is warm/muted/soft-edged (navy `#2E3B42`, sage `#7FA491`, warm cream neutrals, generous radii) — never dilute with generic Material/iOS defaults or saturated "alert" colors.
- Every new primitive needs a smoke test and accessibility props (`accessibilityRole`/`accessibilityLabel`) from the start, not retrofitted later.

## Engineering standards

Beyond the base Rules above: pricing and ride-engine business logic belongs in `packages/domain`, never computed client-side or duplicated between mobile and API — the client only ever displays and enforces bounds the server returned. Background job/queue work (notifications, recurring-pattern detection) should stay scoped to one minimal queue, not a general-purpose framework, until a second genuinely distinct use case justifies more. Any endpoint accepting a client-adjustable value that affects marketplace integrity (price, seats, pickup location) must enforce bounds server-side independent of client-side UI constraints.

## Architecture principles

NOW/NEXT/SCALE horizons are explicit and load-bearing — see `docs/architecture/overview.md`. Do not start SCALE-phase work (PostGIS, read replicas, idempotency enforcement, formal abuse prevention) without a measured trigger (real latency numbers, real concurrent load, real observed abuse) — premature optimization here is itself the risk the SCALE phase exists to avoid. Application-level haversine scanning over a time-windowed row fetch (the current matching approach) is intentionally KEEP, not a stopgap to feel guilty about, through the NOW/NEXT range.

## Domain rules

Full model: `docs/domain/model.md`. Source-of-truth table lives there — check it before assuming where a piece of state belongs. Authoritative state machines (`ride status`, `booking status`) live in `packages/domain` and must never be duplicated in `apps/api` controllers or `apps/mobile`. `route_stops` (ride engine, `docs/domain/ride-engine.md`) and `pricing_configs` (pricing, `docs/domain/pricing.md`) are the two new entities the roadmap introduces — read those documents before touching ride creation, matching, or booking-price logic.

## Documentation conventions

- `docs/product/` — vision, audit, benchmark research.
- `docs/ux/` — principles and target journeys.
- `docs/design-system/` — the formal design-system spec (token values, component inventory, rules).
- `docs/domain/` — domain model, ride-engine design, pricing design.
- `docs/architecture/` — system architecture, scalability strategy.
- `docs/roadmap/` — phased roadmap, status tracker, open decisions, blockers (`docs/roadmap/README.md` is the index — read it first).
- `docs/decisions/` — ADRs for major technical choices.
- When a roadmap phase changes what's true in an audit/design doc, update that doc in the same change — these documents describe current reality, not a frozen snapshot.

## Phase execution rules

The roadmap (`docs/roadmap/README.md`) is structured so a session can be told "Implement Phase N" and act on that phase's file alone. Before starting a phase: read its Prerequisites section and confirm they're actually met (check the status table, don't assume). During: follow its Exact scope — resist adding scope not listed there, even if adjacent and tempting (each phase file calls out its own scope-creep risks explicitly). After: update `docs/roadmap/README.md`'s status table and this file's Current implementation status section in the same change that completes the phase.

## Testing requirements

Every phase's Definition of Done includes: unit tests for new business logic (especially anything touching money, seats, or state transitions — the audit found the current biggest gap is exactly here, e.g. the booking-acceptance race condition), an integration test for the phase's primary flow, and for any change touching the core search→match→book loop, an addition to `tests/e2e` (which today has only a health-check test). `pnpm test`, `pnpm typecheck`, and `pnpm lint` must pass before a phase is considered done.

## Definition of Done (general)

A phase is done when: its own file's Definition of Done checklist is fully checked, tests pass, no screen renders fabricated data after a real mutation, no new free-form entry exists for price or pickup location, and the roadmap status tracker reflects the change. "Looks done" (UI renders, no visible error) is explicitly not the bar — the audit's single worst finding was exactly a screen that looked done while showing fabricated data.

## Current implementation status

Full audit: `docs/product/audit.md`. A newer product spec, `docs/unified_driver_and_passenger_journey.md` ("dynamic corridor-matching," segment-based capacity/pricing, in-progress/live matching, automatic trip-lifecycle inference), has its own reconciliation audit at `docs/product/unified-journey-audit-2026-08-28.md` — read that audit before starting any phase touching matching, pricing, booking concurrency, or trip lifecycle/tracking; it supersedes this section's phase-level detail wherever the two disagree. As of 2026-08-22: Phases 1-11 (the full original NOW/NEXT roadmap) plus Phase 13 (Search & Matching Engine, added post-audit) are implemented. The codebase is materially more mature than a typical MVP — real OSRM routing, a genuinely sophisticated route-aware matching algorithm, a rich domain schema, and a production-grade driver-onboarding flow already exist. Both of the audit's two confirmed highest-priority gaps are addressed: the fake (explicitly non-geospatial) passenger pickup-point picker was retired in Phase 5, and unconstrained pricing was replaced with a computed, bounded suggestion in Phase 6 — though Phase 6's `base_rate_per_km` is explicitly a first-cut value pending business confirmation, not a settled number (see "Important decisions" below). Phase 12 (Scale Hardening) remains the only phase not started — it is trigger-based, not calendar-based, and should not be started without a measured trigger (see "Recommended next phase" below).

**Completed phases:**
- **Phase 1 — Foundation Hardening**: fixed the confirmed booking-acceptance race condition (atomic DB-level seat accounting in `acceptBooking`/`cancelBooking`), added missing DB indexes, registered rate limiting, added a root `ErrorBoundary` + branded loading state, and wired the post-booking screens (`pending`/`pickup`/`live`/`settlement`) to real booking/ride data instead of fabricated mock values. Also fixed a real, separate bug found along the way: `AppError`'s constructor was silently breaking `instanceof` narrowing for every subclass.
- **Phase 2 — Design System: Interaction Layer**: added `BottomSheet`, `Modal`, `Toast`/`ToastProvider`, `Skeleton{Block,Circle,Text}`, `EmptyState`, and `Icon` to `@vaya/design-system`; recalibrated the `elevation` token scale to the brand's actual soft-shadow character and migrated `Card`/`FieldCard`/`ReviewCard`/`StatTile`/`Modal`/`BottomSheet`/`Toast` onto it; added a `haptics` utility wired into OTP verify, booking request, and ride publish (success + error paths); completed an accessibility baseline pass (roles/labels/grouping) across all 19 pre-existing primitives plus the 6 new ones. Each new primitive is proven in a real screen (EmptyState/Skeleton/Toast in `search/results.tsx`, Modal/BottomSheet/Icon in `(tabs)/profile.tsx`'s new logout-confirmation and language-picker interactions — the latter previously a dead tap with no handler at all).
- **Phase 3 — Real Map Rendering Foundation**: `MapCanvas`/`MapPreview`/`MapRoute` are now real `react-native-maps` primitives (MapView/Marker/Polyline) instead of the CSS-art placeholder, with a `SkeletonBlock` shown until `onMapReady` and a `mapTileTint` wash consuming the existing map color tokens. `search/cluster.tsx` already used real maps directly (unchanged); `bookings/pickup.tsx`/`live.tsx` now render real `MapPreview`s using real pickup/destination coordinates threaded through the same param-forwarding chain Phase 1 established. `search/pickup-point.tsx` is explicitly untouched — still the fake pixel-projection screen, deferred to Phase 5 by the phase doc itself. Android needs a real `GOOGLE_MAPS_API_KEY` (see `apps/mobile/.env.example`) to render tiles on-device — a genuine external setup step this session couldn't complete on your behalf.
- **Phase 4 — Ride Engine I: Driver Stops**: new `route_stops` table (migration `0003_wide_squadron_supreme.sql`, additive only) plus `apps/api/src/modules/rides/stop-candidates.service.ts` — samples a ride's OSRM route polyline, snaps each sample to the road network via a new `nearestRoad` (`lib/routing.ts`), classifies road suitability, scores/rejects (max-deviation and motorway rules enforced server-side, never just downranked), clusters within the existing `OVERLAP_CORRIDOR_WIDTH_M` (now exported from `matching.service.ts`), and caches generation by route-polyline hash in Redis. New endpoints `POST /rides/:id/candidate-stops`, `PATCH /rides/:id/stops`, `GET /rides/:id/stops` (public selected-only, `?all=true` driver editing view), and `POST /rides/:id/publish` (ride creation now starts `draft`, reusing the previously-unused `draft` ride status and the existing state machine — see `docs/domain/ride-engine.md`'s Phase 4 implementation notes for why). Mobile's `driver/publish.tsx` gained a second step — a real map with tappable candidate markers (`DriverMapPin`) and a `BottomSheet` detail/toggle view — between the existing form and the actual publish action; publishing with zero additional stops always works. **Genuinely verified, not assumed:** unit tests for the scoring/clustering math (20 cases, no network); an integration test run against the real docker-composed OSRM instance + real Postgres (not mocked); a manual run against 3 real Tunisian routes (urban, suburban, intercity) confirming real road-snapped, named candidates and — for the intercity Tunis→Hammamet route crossing the A1 — correct rejection of motorway-speed samples. **Stated limitation, not a gap that was missed:** OSRM's `nearest`/`route` responses in this deployment carry no way-class tag at all (verified live), so road classification uses local travel speed from route annotations instead of the design doc's originally-assumed metadata field — documented in `docs/domain/ride-engine.md`.
- **Phase 5 — Ride Engine II: Passenger Stop Selection**: retires the single worst finding from the original audit — `apps/mobile/app/search/pickup-point.tsx`'s fake pixel-projection screen (`PX_PER_DEGREE = 9000`, a fixed-degrees-per-pixel demo mapping with no real geocoordinates behind it at all) is fully rebuilt around real ride-engine data: a real `MapCanvas`, numbered pins for a matched ride's ranked candidate stops, closest stop pre-selected by default, `BottomSheet` for stop detail, honest `EmptyState` when nothing is in range — zero pixel-projection code remains anywhere in the file. `matching.service.ts`'s `scoreCandidates` now fetches each candidate ride's driver-selected `route_stops` and ranks them by walk-distance (`rankStopsByWalkDistance`, a pure function unit-tested with no DB/OSRM dependency), filtered to `WIDE_PICKUP_RADIUS_M`; each `MatchCandidate` gains `rankedStops` and a `pickupViable` flag. **Zero-viable-stops product decision, made explicitly:** a ride with real stops but none walkable for the passenger is *included* in results flagged `pickupViable: false` rather than silently excluded — this keeps the `pickup_no_viable_stop` analytics event observable through the existing mobile `trackEvent` hook without a second server-side analytics mechanism, while `search/cluster.tsx` still never renders or lets a passenger tap into a non-viable result, so "never offer an impossible pickup" holds at the UI layer regardless (full reasoning in `docs/domain/ride-engine.md`). `bookings.pickup_stop_id` (nullable FK → `route_stops`, `ON DELETE SET NULL`, migration `0004_common_bill_hollister.sql`) is enforced server-side in `bookings.service.ts`: a ride with any selected stops requires a valid same-ride `pickupStopId` and rejects free-form coordinates (400 either way); a ride with zero stops keeps the old free-form flow working unchanged — verified by a dedicated integration test covering all three cases. `explore.tsx`'s pre-search "adjust pickup on map" entry point (which pointed at the old fake screen before any ride was even selected, incompatible with the new screen's required `rideId`) was removed as a direct, necessary consequence of the scope change. **Genuinely verified, not assumed:** unit tests for the ranking/viability pure functions and the mobile stop-selection helpers; an API integration test against real Postgres; and the first real core-loop E2E coverage `tests/e2e` has ever had — a Playwright suite exercising the full HTTP flow (driver onboarding → ride creation → real OSRM-backed candidate generation → stop selection → publish → passenger matching search → booking with `pickupStopId` → both rejection cases → legacy free-form booking) against a live server, real Postgres, and real docker-composed OSRM — all 8 E2E tests (health-check + new suite) passing.

- **Phase 6 — Pricing Engine**: replaces `driver/publish.tsx`'s old unbounded ±1 DT stepper with a computed, bounded suggestion. New `packages/domain/src/pricing` module (`computeSuggestedPrice`, pure/unit-tested) implements `recommended = baseRatePerKm × distanceKm + timeComponentPerMin × durationMin`, an absolute floor for short rides, and a wider min/max spread when the route is a haversine fallback rather than real OSRM data. New `pricing_configs` table (migration `0005_moaning_spiral.sql`), seeded with one active `national` row; `platformFeeRate` defaults to 0 and is not read anywhere — mechanism only, not activated. `POST /rides` now makes `contributionPerSeat` optional (defaults to `recommended`) and returns `{min, recommended, max}` + `routeIsEstimate` alongside the ride; a new `PATCH /rides/:rideId` (draft-only) lets the driver commit an adjusted price, with the server independently re-deriving and re-validating the bound both times — a direct API call with an out-of-bounds value is rejected with a 400 regardless of client behavior (verified by a real-Postgres/real-OSRM integration test, and the existing `tests/e2e` core-loop suite still passes unchanged). Mobile gained a new `PriceRangeStepper` primitive (`packages/design-system`) and a `form → price → stops` flow in `driver/publish.tsx` that makes an out-of-bounds price unrepresentable in the UI. `apps/api/src/db/seed.ts`'s seeded routes/rides/bookings pricing is now entirely formula-derived from real OSRM geometry, not hand-typed — one source of truth. **`base_rate_per_km = 0.25` DT/km and `time_component_per_min = 0.08` DT/min are an autonomously-derived first-cut (Tunisian fuel price × typical compact-car consumption × a cost-sharing markup, documented in `docs/domain/pricing.md`'s "Rate derivation" section), explicitly pending business confirmation — do not treat this as a settled monetization/pricing decision.**
- **Phase 7 — Notifications Foundation**: gives the already-modeled `notifications` table a real delivery mechanism. New `device_tokens` table (migration `0006_greedy_hedge_knight.sql`, one row per user+device, `token` globally unique) + `POST /users/me/push-token`; new `apps/api/src/modules/notifications` module (`GET /notifications`, `PATCH /notifications/:id/read`, dispatch service). First (and, per this phase's explicit scope, only) background job queue in the codebase: one BullMQ queue (`apps/api/src/lib/queue.ts`, its own Redis client pointed at the same `REDIS_URL` as `lib/cache.ts` — a second logical client, not a second Redis, since BullMQ requires `maxRetriesPerRequest: null`), consumed by one standalone worker process (`apps/api/src/worker.ts`) — not a general multi-queue framework. Push send is a direct HTTP call to Expo's push API (`modules/notifications/expo-push.ts`), not `expo-server-sdk` — dependency-free for a 3-event-type minimal dispatch path. `bookings.service.ts`'s `createBooking`/`acceptBooking`/`declineBooking` each gained a best-effort notification-row-creation hook around their existing logic (Phase 1's atomic seat-accounting in `acceptBooking`/`cancelBooking` untouched); a push-send failure is logged and retried via BullMQ's native retry and can never fail the triggering booking call — verified with a real-Postgres/real-Redis integration test that mocks only the Expo HTTP call, not just by inspection. Mobile: `expo-notifications` added; new `apps/mobile/src/services/notifications/` (token registration, contextual permission-prompt timing gated to once per install, foreground-Toast + tap-deep-link wiring, notification copy) and a minimal inbox at `apps/mobile/app/notifications/index.tsx` (list, unread `Badge`, mark-as-read on tap — a `Row`/`Text`/`Icon`/`Badge` composition, no new design-system primitive, per the phase doc). Push permission is requested contextually — after a driver's first ride publish or a passenger's first booking, never on cold start. Deep-linking is scoped to exactly the 3 dispatched event types (all currently resolving to the existing `(tabs)/trips` screen — a dedicated driver "review this request" screen doesn't exist anywhere in this codebase yet, a real gap, not built here). **Deferred, real external-service setup this environment cannot complete** (same category as Phase 3's Google Maps key / Android verification note): live push delivery to a physical device needs real Expo/EAS push credentials (EAS project id, APNs/FCM certs) this sandboxed environment has none of — the full code path is wired and tested with the Expo push HTTP call mocked, but **on-device push delivery verification is an explicit deferred manual step**, not something to claim as done. Full notes: `docs/roadmap/README.md`.

- **Phase 8 — Messaging**: new `conversations`/`messages` tables (migration `0007_wild_sunset_bain.sql`, additive, indexed on `(conversationId, createdAt)`) and `apps/api/src/modules/conversations` module (`GET /conversations/:bookingId`, `GET /conversations/:id/messages?since=`, `POST /conversations/:id/messages`). One conversation per booking — never per ride — auto-created the moment a booking reaches `accepted` (hooked into `bookings.service.ts`'s `acceptBooking`, Phase 1's atomic seat-accounting untouched) and closed immediately when its trip is cancelled (`cancelBooking`'s existing trip-cancellation branch). Authorization (only the booking's driver and rider) and the read-only-after-terminal-trip rule are both enforced on every single request, re-derived live from `trips.status` rather than trusted from the cached `conversations.status` column alone — this codebase had no trip-completion endpoint yet at the time (Phase 9 added one), so the live check is what makes the guarantee hold correctly once one ships. Extends Phase 7's dispatch with a new `message_received` event type through the same BullMQ queue/Expo-push path, no second dispatch mechanism. Mobile: new `MessageBubble` design-system primitive, new `apps/mobile/app/conversations/[bookingId].tsx` (polling via RTK Query's `pollingInterval`, no WebSockets), with a "Message" entry point on `bookings/pending.tsx`/`pickup.tsx`/`live.tsx` (the rider-side screens the phase doc names) — there is still no driver-side per-booking screen anywhere in this codebase to add an equivalent entry point to (same pre-existing gap Phase 7's notes documented for deep-linking), a real limitation carried forward, not built here. Content-moderation/reporting was deliberately not built (Open Decision #6, still open). Full notes: `docs/roadmap/README.md`.

- **Phase 9 — Ratings, Reviews & Trust**: closed the loop on the already-correctly-shaped `ratings` table with a real submission flow, aggregation, and trust-tier surfacing. `POST /trips/:id/ratings` (new `apps/api/src/modules/ratings` shape, `tripId` now in the URL) enforces one rating per `(tripId, raterUserId)` and a server-side 24h submission window (both 409s), computed via new pure `packages/domain/src/rating` modules (`computeRatingAggregate` — recompute-from-all-ratings, not increment/decrement; `computeTrustTier` — "Nouveau"/"Confiance"/"Top VAYA", VAYA's own naming; `isWithinRatingWindow`). `GET /users/:id/trust-summary` returns the public-safe `{tier, ratingAvg, tripCount, punctualityScore}` shape only — never raw comments. **Two decisions this phase made autonomously (no human available):** (1) **rider-reputation storage** — a new dedicated `rider_profiles` table (migration `0008_yellow_shen.sql`), mirroring `driver_profiles`' aggregate shape, chosen for symmetry with the existing driver-aggregation pattern, churn isolation from `users`, and lazy one-to-one creation — resolves the roadmap's previously-open technical-debt item. (2) **Trip completion, a necessary scope addition, verified before building rather than assumed**: this codebase had no trip-completion mechanism at all (confirmed — no caller of `canTransitionTripStatus` existed anywhere outside `packages/domain`'s own tests), so a minimal `POST /trips/:id/complete` (new `apps/api/src/modules/trips` module) was added — callable by either trip party (no driver-side trip-execution screen exists yet, unlike a literal "driver-only" reading of the phase doc), transitioning via `packages/domain`'s trip-status state machine, extended to allow `completed` directly from any non-terminal status since the mobile trip-progress flow (`pending → pickup → live → settlement`) is still a presentational mock with no live position feed. Also fixed a related, previously-unnoticed gap: `driver_profiles.tripCount` was only ever written by `seed.ts`, never by a real trip completing — `completeTrip` now recomputes it (and the new `rider_profiles.tripCount`) directly from `trips` rows. Mobile: new `StarRatingInput` design-system primitive (no equivalent existed) and an extended `Chip` (`onPress`/`selected`) so the rating sheet's punctuality toggle no longer needs raw local styling; `RatingPromptSheet` (`BottomSheet`-based) reachable from a rebuilt `bookings/settlement.tsx` (now fetches the real trip, best-effort-completes it, and actually submits — previously a dead "Terminer" button wired to nothing) and from a new global `RatingPromptBridge` (`app/_layout.tsx`) polling `GET /trips/pending-rating` once per cold start for the "re-surface once if unsubmitted" behavior. Trust-tier badges wired into `search/trust.tsx` (as the phase doc specified) and `search/cluster.tsx`'s recommended-driver CTA — **deviation from the phase doc's literal wording, documented**: `search/results.tsx` shows only anonymous cluster counts with no per-driver identity on screen, so `cluster.tsx` (this app's actual per-driver "result card") got the badge instead. All 4 analytics events fire via the existing `trackEvent` hook. Full notes, including both decisions' complete reasoning: `docs/roadmap/README.md`, `docs/domain/model.md`.

- **Phase 10 — Cancellation & No-Show Policy**: built the cancellation/no-show flow that didn't exist anywhere before this phase, with an explicit, VAYA-designed reputation-based policy (no payment system exists to build a BlaBlaCar-style refund policy against — full design and reasoning in `docs/domain/cancellation-policy.md`). **Tiers** (`packages/domain/src/booking/cancellation-policy.ts`, pure/unit-tested): ≥24h before departure is free (no consequence); <24h but ≥30min is "moderate" (+1 weighted reliability-penalty point, no rating impact); <30min or after departure is "severe" (+3 points). A separately-reported no-show (not a cancellation tier) is weighted heavier still — +5 points and an automatic 1-star rating for the no-show party via Phase 9's rating mechanism — and can only be reported once at least 15 minutes have passed `departureAt` (server-enforced `canReportNoShow`, independent of the mobile UI's contact-first guidance text). New `reliabilityPenaltyPoints` integer column on both `driver_profiles`/`rider_profiles` (migration `0009_nasty_boomerang.sql`, additive) — deliberately separate from the rating-derived `reliabilityScore`, which Phase 9's aggregate recompute would otherwise silently overwrite. **A second seat-accounting race found and fixed along the way** (not assumed safe, verified): `cancelBooking`'s pre-existing status-transition guard was a stale-read check only, unconditional on the actual `UPDATE`'s `WHERE` clause — two concurrent cancels on the same booking could both pass and both double-credit `seatsAvailable`; fixed by re-validating `status` inside the `UPDATE ... WHERE` clause itself, mirroring Phase 1's `acceptBooking` fix, applied to both `cancelBooking` and the new `reportNoShow`, proven by a concurrent-cancel integration test. New `GET /bookings/:bookingId/cancellation-preview` (read-only, shown before the destructive action) and `POST /bookings/:bookingId/report-no-show`; `POST /bookings/:bookingId/cancel` now returns the applied policy alongside the booking. Mobile: `CancellationSheet`/`NoShowReportSheet` (`apps/mobile/src/features/bookings/`, `BottomSheet`-based, no new design-system primitive) wired into every active-booking screen that exists today — `(tabs)/trips.tsx`, `bookings/pending.tsx`/`pickup.tsx`/`live.tsx` — **deliberately rider-only at every entry point** (documented, not assumed): this codebase still has no driver-side per-booking screen, the same gap Phases 7-9 already flagged; the backend supports either party. Full notes: `docs/roadmap/README.md`.

- **Phase 11 — Recurring Rides**: built the first consuming logic for the `recurring_patterns` table, which existed in the schema (`detected → suggested → enabled → dismissed` lifecycle, `confidenceScore`) since the original audit but had zero consuming logic before this phase. A periodic detection job (`apps/api/src/modules/recurring`) clusters a user's ride/booking history by corridor via a new pure `packages/domain/src/recurring/detect-recurring-patterns.ts` and writes/refines `detected`/`suggested` rows; `GET /recurring-patterns` and `PATCH /recurring-patterns/:id` (enable/dismiss) expose them. **Reuses Phase 7's one BullMQ queue as a second job type** (`recurring-pattern-scan`, routed by `job.name` in `worker.ts`), per CLAUDE.md's "one minimal queue... until a second genuinely distinct use case justifies more" — not a second queue. Detection thresholds are externalized in a new `recurring_detection_configs` table (migration `0010_magenta_molly_hayes.sql`), mirroring Phase 6's `pricing_configs` pattern exactly, with a pure `DEFAULT_RECURRING_DETECTION_CONFIG` fallback. A dismissed pattern isn't permanently suppressed nor freely re-suggested — a later scan only resurrects it once its freshly recomputed `confidenceScore` clears the score it had at dismissal by a configured delta, reusing the existing column rather than adding a trip-count column. An `enabled` driver pattern's auto-draft **reuses the existing `POST /rides` endpoint** (a new optional, server-validated `recurringPatternId` field) rather than a bespoke endpoint, confirmed via a lightweight one-screen mobile flow (`app/recurring/confirm-draft.tsx`) that never auto-publishes; an `enabled` rider pattern's proactive matching **extends `matching.service.ts`** with `findBestMatchForRecurringPattern` (reuses `searchRides`'s existing ranking/viability logic, no parallel implementation) and fires the already-modeled `recurring_proactive_match` notification via Phase 7's dispatch path. Mobile: `RecurringPatternPromptBridge`/`RecurringPatternPromptSheet` (mirroring `RatingPromptBridge`'s poll-once-per-cold-start pattern) surface the proactive prompt; `app/recurring/index.tsx` is the pattern-management list screen. **Deliberate deviation from the phase doc's example copy**: the prompt never quotes a specific trip count ("Vous avez pris cet itinéraire 3 fois...") since `recurring_patterns` doesn't store trip count, only `confidenceScore` — inventing a number would violate the "never show fabricated data" principle. Full notes: `docs/roadmap/README.md`.

- **Phase 13 — Search & Matching Engine** (added post-audit, 2026-08-22): fixes the core structural gap between VAYA's matching and BlaBlaCar's actual mechanic — `matching.service.ts` used to compare a rider's origin/destination only against a ride's own endpoints, so a ride with a real driver-selected stop mid-route was invisible to a search for that sub-trip no matter the radius. `searchRides` is now a 5-tier cascade (`exact → wide_corridor → route_passthrough → closest_departure → none`) returning `{tier, candidates, message}` from one consolidated endpoint (`GET /matching/corridor-fallback` retired). **`route_passthrough`** (new `projectPointOntoRoute` in `lib/polyline.ts`) finds rides whose real OSRM route runs through the rider's corridor even when the ride's own endpoints are far away, direction-checked and only ever bookable through real `route_stops` on both ends. **`closest_departure`** returns the nearest upcoming departure(s) on the same corridor within a 14-day lookahead when nothing matches near the requested time — the literal "no ride at that time, here's the closest" behavior. New symmetric **dropoff-stop selection** (`bookings.dropoff_stop_id/label/lat/lng`, migration `0013_last_forge.sql`, additive/nullable) mirrors the existing pickup-stop mechanism so a pass-through booking can actually end mid-route, not just start there. Full notes, including the deliberately-deferred multi-leg and partial-direction matching ideas: `docs/roadmap/README.md`, `docs/roadmap/phase-13-search-engine.md`.

**Current phase:** none in progress.
**Recommended next phase:** None of the original numbered roadmap — Phases 1-11 and 13 are complete, Phase 12 (Scale Hardening) stays trigger-based per its own doc. The Google Maps Platform + PostGIS initiative (`docs/product/google-maps-platform-implementation-report-2026-08-23.md`) has progressed further than its own doc states: Phases 1-4 (location/routing provider abstraction, PostGIS spatial layer, a real `detour_match` tier) are done, **and the Phase 5-8 active-trip/live-GPS-journey system — previously tracked here as "zero existing infrastructure" — has since been built and merged to `main`** (driver GPS broadcast, WebSocket + Redis live tracking, automatic proximity-based lifecycle transitions, and a BullMQ staleness sweep that force-completes abandoned trips; see `packages/domain/src/trip/`, `apps/api/src/modules/trips/`, `apps/mobile/src/features/tracking/`). This was not backfilled into this file when it landed — corrected 2026-08-28. It is **not** yet wired into matching/search (an `in_progress` ride is still fully invisible to search rather than restricted to its remaining corridor), and pre-boarding passenger-facing tracking currently exposes the driver's raw GPS earlier than intended — both are open findings, not resolved. A new product spec, `docs/unified_driver_and_passenger_journey.md`, and a full reconciliation audit against it, `docs/product/unified-journey-audit-2026-08-28.md`, supersede the driver-side per-booking-screen gap below as the source of next-phase priorities — read the audit's P0/P1 list before picking the next phase.

**Post-Phase-11 fixes (not part of the numbered roadmap, done in the same worktree/branch):**
- Fixed the OTP verification screen (`apps/mobile/app/(auth)/otp.tsx`): the code field was a visually-hidden 1×1px `TextInput` relying only on `autoFocus`, with the visible cell UI purely decorative and no way to refocus it — the screen's tap-to-dismiss-keyboard handler meant tapping the OTP cells dismissed the keyboard instead of reopening it, leaving users stuck once they closed it. Fixed by wrapping the visible pill in a `TouchableOpacity` that focuses the input via `ref`.
- **Added a real departure date/time picker to ride creation and ride search** — previously `rides.departureAt` was fully modeled end-to-end but the mobile UI never actually exposed it: `driver/publish.tsx` only offered 4 fixed relative-minute presets (max 2h out, so a driver literally could not publish tomorrow's ride), and `explore.tsx`/`search/results.tsx` always searched "now" with no way to look for a future ride at all. New `DepartureTimeSheet` primitive (`packages/design-system`, `utils/scheduling.ts` for the pure day/time-grid math, unit-tested) is a `BottomSheet` of day chips (Aujourd'hui/Demain/next 14 days) over a half-hour time grid — never free-form entry, per this file's own product principle #1 — that never offers an already-past slot. `driver/publish.tsx` keeps its original quick-preset chips as one-tap shortcuts and adds a "Choisir une date" trigger for arbitrary future scheduling, plus a `departureAt`-in-the-future guard on the Continuer button. `explore.tsx` gained a "Quand ?" field (Maintenant/Dans 1h/Cet après-midi/Demain matin presets + the same full sheet) writing to a new `search.desiredDepartureAt` slice field that `startSearch` now uses instead of always capturing `new Date()`; `results.tsx`'s "Me notifier" demand-signal window is now centered on the actual searched time instead of always the current moment, and its header shows the searched date/time back to the rider. No backend changes needed — `POST /rides`'s `departureAt` and `GET /matching/search`'s `when` already accepted an arbitrary timestamp; only the mobile UI was missing.
- **Made the profile screen's Apparence setting functional** (previously a dead row whose tap only showed an "it follows your system" toast): profile.tsx now opens a real `BottomSheet` (Système/Clair/Sombre, mirroring the Langue sheet's pattern) writing to a new `appearance` Redux slice (`src/state/appearanceSlice.ts`, hydrated from SecureStore via `src/services/settings/appearanceStorage.ts` with validated fallback to `'system'`). `_layout.tsx` was reordered so `ReduxProvider` wraps the theme provider — a new `ThemedApp` component reads the slice and passes `'system'` as no-override (provider follows `useColorScheme()`) or pins `'light'`/`'dark'`; picking a scheme flips the whole themed UI live, including the status-bar icon polarity (`ThemedStatusBar`) and explore's `StatusBarBlend` frost. Same-change fix along the way: explore's old flat dark scrim over the map (a gray band behind the OS clock/battery/wifi) was replaced by the new progressive-frost `StatusBarBlend` primitive (`packages/design-system`, five pixel-snapped blur bands + theme-background wash, sized to exactly `insets.top` + breathing room), smoke-tested; legacy-token screens outside the Stitch rider flow keep their static light palettes until migrated.
- **Recreated the Stitch "Vaya Passenger Journey UX" rider search flow pixel-for-pixel, with a real app-wide dark mode** (resolves Open Decision #5). New `@vaya/design-system` `theme/palette.ts` + `AppThemeProvider`/`useAppTheme()` — a second, separate light/dark token pair (cool lavender-white/near-black/emerald-mint, pulled from the Stitch reference; `darkPalette` derived from Material-3's own inverse/fixed-surface roles since the mockups were light-only) — mounted once at the app root, following `useColorScheme()`. Rebuilt to match: `(tabs)/explore.tsx` (Search Ride), `search/composer.tsx` (restyle only), `search/results.tsx` (Ride Results Refined — replaced the scatter-bubble cluster UI with a real sorted driver-card list; new `DriverListCard` primitive), `search/cluster.tsx` (Route Map — now shows every real candidate from the current search, not one label-filtered group; new shared `useOpenDriver()` hook so results.tsx and cluster.tsx resolve a tapped ride identically), `search/trust.tsx` (Driver Profile — added real `bio`/`vehicle.plateNumber` to the public-profile API response, computed "Fiable"/"Top VAYA" pills from real `reliabilityScore`/trust tier instead of static badges), and `bookings/confirmed.tsx`/`bookings/pending.tsx` (Request Sent / Ride Confirmed — note the filename mismatch predates this change: `confirmed.tsx` is actually the pre-acceptance waiting screen, `pending.tsx` is actually the post-acceptance one; `confirmed.tsx` was a fake 1.6s auto-advancing animation and is now a real held screen that polls `listMyBookings` and only advances once a driver has genuinely accepted). New `search/reviews.tsx` is the one deliberately mock screen in this set — Phase 9 keeps rating comments private to the trip's two parties by design, no public per-review endpoint exists, and adding one wasn't this change's call to make. "Call Driver" is rendered disabled everywhere it appears (no phone number is ever exposed via the public API) and pre-booking "Message Driver" is disabled with an explanatory state (messaging is booking-scoped, created only after acceptance, Phase 8). Full reasoning and the complete screen-mapping table: `docs/design-system/README.md`'s Colors section.
- **Completed the Publish Ride flow against `stitch/publish_ride/*` and added the missing verification hand-off from `stitch/verification/*`** — the wizard's origin/destination/date-time/stop-selection/price steps already matched Stitch's intent functionally (real `search/location.tsx`, `DepartureTimeSheet`, real OSRM-backed stop selection — none rebuilt), but a real "Review & Publish" step (`publish-final-review.html`) didn't exist anywhere: `driver/publish.tsx`'s old `stops` step called `finalizePublish()` directly with no summary screen in between. Added a 4th `review` step to the same single-file wizard (kept local `useState`, matching this file's own established "one route, no cross-route state" pattern) — a bento-style summary (itinerary timeline with the driver's selected stops, price, seats, vehicle), each section deep-linking back to its editor. `finalizePublish` gates on `verificationStatus === 'approved'` (new pure `isVerifiedDriver`, `src/features/driver-publish/verificationGate.ts`, unit-tested) before publishing; failing that shows a `BottomSheet` matching `publish-verification-requirement-prompt.html` ("Commencer la vérification" / "Plus tard"). **Reachability note, documented in code**: `drivers.service.ts`'s `createOnboarding` always sets `verificationStatus: 'approved'` synchronously (no admin review queue exists), so this specific gate in practice only ever fires for "hasn't onboarded yet" — never a real `pending`/`rejected` profile — but is wired as a real, independent check against the schema's full state space rather than assumed away.
- **The publish tab now opens the ride-creation wizard for every user, not just onboarded drivers** — `(tabs)/publish.tsx` previously redirected anyone without a `driverProfile` straight into `/driver/onboarding/vehicle`, so the whole ride-creation UI (and thus the new review screen above) was unreachable until a driver had already completed KYC; `driver/publish.tsx` itself had no path through it that didn't assume a vehicle already existed (`createRide`'s `vehicleId` is `NOT NULL` at the DB level — not something to relax without a real backend migration). Now: the tab always opens `driver/publish.tsx`; the `form` step (origin/destination/date/seats) needs no vehicle at all, and a driver with none skips straight from `form` to `review` (2-step progress instead of 4, since `price`/`stops` genuinely have nothing to show without a real server-computed ride) — the review screen shows real itinerary/seats data but an honest locked "Après vérification" placeholder for price and "vous ajouterez votre véhicule…" for the vehicle card, never a fabricated number. Tapping "Publier ce trajet" with no vehicle goes straight to the same verification-requirement `BottomSheet` as above. `driverOnboardingSlice` gained `pendingRideDraft` (raw origin/destination/departureAt/seats, alongside the existing `pendingRide` rideId-only case) so `onboarding/selfie.tsx`'s `submit()` can create *and* publish the real ride the instant a real vehicle exists (right after `createOnboarding` succeeds), then hand off to a purely presentational `driver/onboarding/confirmation.tsx` (`verification-confirmation-pending-state.html`) that reports via a `status` param whether that auto-publish actually succeeded — it never claims success it can't back with a real mutation result. `(tabs)/trips.tsx`/`profile.tsx`'s existing "Become a driver" CTAs are unchanged — they already dynamically relabel themselves per `driverProfile` and remain the standalone, ride-independent entry into verification.
- **Rebuilt the publish + verification screens' visuals on the same Stitch-matching theme system PR #10 landed for the rider search flow**, rather than leaving them on the old static `colors` tokens next to newly-Stitch-matched rider screens. Reuses `useAppTheme()`/`GlassSurface` exactly as the rider flow established (hand-rolled header/CTA chrome per screen, no retrofit of the old `ScreenHeader`/`StepProgress`/`Button`/`RoutePulseBadge`/`FieldCard`/`FieldRow` primitives — none of those gained a `theme` prop, matching the rider flow's own precedent of hand-rolling rather than fighting unmigrated primitives). Covers: `driver/publish.tsx`'s all 4 steps + the verification-requirement `BottomSheet` (now themed via `BottomSheet`'s new optional `theme` prop), `driver/onboarding/confirmation.tsx` (pulsing badge mirrors `bookings/confirmed.tsx`'s hand-built ring+badge pattern, in amber/`colors.warning` rather than `theme.accent` since this is a pending-verification state, not success), `driver/onboarding/vehicle.tsx` (the plain gray-card form directly flagged as "the old design"), and `driver/onboarding/selfie.tsx`'s post-capture review screen. Initially left `driver/onboarding/index.tsx` and `CaptureCamera.tsx` untouched (both were fixed dark/navy full-bleed treatments) — revisited one pass later, see below. `packages/design-system`'s `Input` primitive had no `theme` prop at this point (a real, pre-existing gap) — `vehicle.tsx`'s text fields stayed light-styled inside an otherwise themed screen; migrated in a later pass, see below.
- **Fixed a real navigation bug and finished the remaining "legacy" pieces of the publish + verification flow.** `driver/publish.tsx`'s back arrow did nothing on the wizard's first step: the publish tab *replaces* into this screen ((tabs)/publish.tsx), leaving no history entry for `router.back()` to land on; `handleBack` now checks `router.canGoBack()` and falls back to `router.replace('/(tabs)/explore')`. The Départ/Arrivée fields opened `search/location.tsx` — an un-migrated, still-static-`colors` screen — while the rider search flow had already moved to a Stitch-matched `search/composer.tsx` accepting the exact same `field`/`searchSlice` contract; repointed both taps to `/search/composer` and deleted `search/location.tsx` entirely (confirmed zero remaining references, including its `Stack.Screen` registration in `search/_layout.tsx`). The date/time picker similarly still used the older `DepartureTimeSheet`; swapped for the same `DateCalendarSheet`/`TimeWheelSheet` pair and Date/Heure `paramBtn` grid `explore.tsx` uses, reading/writing the screen's own local `departureAt` (not `searchSlice`, which is rider-only) — both sheets already merge onto the existing `value`, so this needed no new date-math. Revisited the earlier "no Stitch reference" call on `driver/onboarding/index.tsx` (its plain white benefit-cards → `GlassSurface`, hand-rolled CTA) and on `CaptureCamera.tsx` (the shared capture chrome behind `license.tsx`/`insurance.tsx`/`selfie.tsx`'s capture phase): reading `stitch/verification/verification-identity-selfie.html` and `verification-license-upload.html`/`-insurance-proof-b.html` directly showed real target designs after all — a light background, a *framed* (not full-bleed) viewfinder (circular w/ pulsing ring + SVG face guide for the selfie shape, a card-framed live view w/ corner brackets for the document shape), and a bento-style guidance-tip list under it, now built with a new `tips: CaptureTip[]` prop each of the 3 capture screens supplies its own copy for. **One deliberate divergence, not a miss**: Stitch's document screens (`license-upload.html`, `insurance-proof-a/b.html`) are actually a *fake gallery-upload* form — a dashed drop-zone, no camera, `-a`'s "Extracted Details" are hardcoded fabricated OCR fields (`State Farm Insurance`, `POL-987654321`) with no backing OCR pipeline anywhere in this codebase. Copying that mechanism would both regress a real, protected differentiator (CLAUDE.md: "Documents en direct — jamais depuis la galerie") and fabricate data — so the real live `CameraView` was kept and only its surrounding chrome adopted Stitch's light visual language.
- **Moved the ride-creation wizard to actually live at `(tabs)/publish.tsx`, so the bottom tab bar stays visible through the whole flow like every other tab.** Previously `(tabs)/publish.tsx` was a stub that immediately `router.replace`d to `driver/publish.tsx` — a route outside the `(tabs)` group, so the moment a driver tapped "Publier" the tab bar vanished for the rest of the flow (this is also what made the earlier back-arrow fix moot: within a tab, "back" only ever means "previous wizard step," never "leave the tab" — a separate affordance the tab bar itself already provides). The entire wizard (form/price/stops/review, the verification-requirement `BottomSheet`, all local state/handlers) now renders directly in `(tabs)/publish.tsx`; `driver/publish.tsx` is deleted outright, its `Stack.Screen` registration dropped from `driver/_layout.tsx`, and every remaining `/driver/publish` reference (`trips.tsx`/`profile.tsx`'s CTAs, `selfie.tsx`'s post-onboarding redirect, and the source-level regression-guard test in `notification-foreground-and-permission-timing.test.ts`) repointed to `/(tabs)/publish`. Also removed the thin `StepProgressBar` line under the header on every step — flagged as visually inconsistent with the rest of the app, and no other screen in this codebase uses that pattern. The review screen's CTA is no longer unconditionally "Publier ce trajet": for a driver with no vehicle yet (`!hasRideData`), tapping it doesn't publish anything — it opens the verification-requirement sheet — so the label now reads "Vérifier mon profil pour publier" (rocket icon → shield icon, terms hint → an honest "verification takes about 5 minutes" line), reserving "Publier ce trajet" for when it's literally true.
- **Migrated `packages/design-system`'s `Input` primitive to the theme system** (a new optional `theme?: AppPalette` prop, following the exact `GlassSurface`/`BottomSheet`/`Chip` precedent — defaults to the legacy static `colors` look when omitted, so every other consumer is unaffected) and wired it into `driver/onboarding/vehicle.tsx`'s 4 fields. This was the one real remaining rough edge flagged directly against a device screenshot: on a dark-mode device, `vehicle.tsx`'s fields rendered as flat opaque-white boxes — correct per the (undocumented-until-now) legacy default, but visually jarring inside an otherwise fully dark-themed `GlassSurface` card. Themed, the field is a bordered, `theme.surfaceMuted`-tinted recessed box (border brightens to `theme.ink` on focus) instead — consistent with how `paramBtn`/chip elements elsewhere in this same pass sit inside a `GlassSurface` parent. `vehicle.tsx` was confirmed to be the only consumer of `Input` anywhere in `apps/mobile` before changing it, so no other screen was at risk.
- **Rebuilt `app/index.tsx` (the landing/sign-in screen) against the Stitch "Vaya Landing" project** — fetched live via the Stitch MCP tools (`vaya-landing-premium-dark-mode.html` + its companion `contextual-authentication-sheet.html`, both saved to the untracked local `stitch/landing/` reference folder alongside the project's other Stitch exports) rather than the local `stitch/` HTML files this whole session had otherwise worked from. **Two deliberate divergences from the literal mockups, both forced by what this backend actually has**: (1) both references show Google/Facebook/Instagram OAuth buttons — this app has no social login and no separate sign-up step at all (`POST /auth/otp` + `POST /auth/otp/verify` always create-or-log-in by phone number, unconditionally), so the buttons became the one real mechanism instead: a phone-number field with the existing `+216` country pill, submitting into the unchanged `(auth)/otp.tsx` flow; (2) the reference's full-bleed cinematic photo has no equivalent asset in this codebase and wasn't fabricated/sourced — replaced with an on-brand ambient glow (two soft `darkPalette.accent`/`accentGlow`-tinted blurred circles) behind the existing `RoutePulseBadge` hero motif already used across the driver-onboarding flow. **"Subtly expandable," matching the second reference exactly**: the landing screen itself stays a single hero + one "Commencer" CTA (no phone field visible up front); tapping it slides up a `BottomSheet` (themed via the fixed `darkPalette` import directly — not `useAppTheme()` — so the sheet stays visually consistent with the hero's always-dark treatment regardless of the device's system theme, the same fixed-brand-dark choice `driver/onboarding/index.tsx`'s hero already makes) containing the real phone-entry step. The pre-existing `accessToken` → `Redirect href="/(tabs)/explore"` guard is unchanged — a signed-in user already lands on the search tab, exactly as asked. This also resolves the audit's long-open "arc." vs "VAYA" naming flag (see "Important decisions" above) — the old screen's bespoke `GlassArcHero` SVG and "arc." wordmark are gone; the new one says "VAYA" throughout, matching everywhere else in the app.
- **Rebuilt `packages/design-system/src/theme/palette.ts`'s `lightPalette`/`darkPalette` to derive from `tokens/colors.ts`'s existing navy/sage/cream palette, instead of the generic Material black/white/emerald-mint the "Vaya Passenger Journey UX" Stitch export originally carried.** Every screen already migrated to `useAppTheme()` (the whole rider search flow, plus this worktree's own publish/verification/landing rebuilds) had `ink`/`onInk`/`background` sitting on flat `#000000`/`#FFFFFF`/`#F8F9FF` and `accent` on a Material emerald `#006C49` — exactly the "generic Material default" this file's own design-system rules warn against, and visually a second, unrelated brand next to every screen still on the static `colors` tokens. Every `AppPalette` role is now sourced from `colors.ts` instead (light: `background`/`ink`/`onInk` → `gray100`/`primaryDark`/`gray50`, warm cream-and-navy, never flat black/white; dark: → `primaryDark`/`gray100`/`primaryDark`, the same navy family `driver/onboarding/index.tsx`'s hero already uses; `accent`/`accentStrong`/`accentGlow` → the `secondary` sage family in both modes, not Material emerald) — the `AppPalette` *interface* (role names) is untouched, so this was a values-only change with zero consumer code edits required. One explicit, deliberate choice: `onAccent` is a dark navy (`primaryDark`) in *both* light and dark mode, not white — sage (`#7FA491`/`#A8C4B6`) is a mid/light tone in both palettes, so white text/icons on a solid accent fill would have real contrast problems; dark-on-sage reads as more premium/boutique than Material's white-on-saturated-green convention anyway. Regenerated the (real, content-changed — not the usual line-ending noise) snapshots this rippled into: `search-flow-primitives.snapshot.test.tsx` (design-system) and `search-composer-screen`/`search-results-screen`/`search-reviews-screen`/`search-trust-screen` (mobile) — all 226 tests across both packages pass, aside from the pre-existing, unrelated `date-time-sheets.snapshot.test.tsx` failure (confirmed pre-existing by reverting just this change and re-running: it fails identically either way, a `GestureHandlerRootView` nesting mismatch inside `BottomSheet`, not a palette issue).
- **Superseded the above with a genuinely from-scratch premium palette, on explicit direction to stop reusing the existing tokens and actually redesign.** Remapping `AppPalette` onto `tokens/colors.ts`'s navy/sage was safe but not what "premium" asked for — it was still muted, safe, unremarkable. The replacement is a deliberate concept, not a recombination: **deep charcoal-emerald darkness meets warm ivory light, unified by one saturated jewel-tone emerald** (`#2E9E6C` light / `#3FBE85` dark — meaningfully more saturated/confident than the old muted sage, reads as a gem rather than an "eco-app" green) rather than a flat black/white/generic-green scheme. Both modes stay warm rather than clinical (`ink`/`background` are never flat `#000000`/`#FFFFFF` in either mode — dark background `#0D1512`, light background a warm ivory `#F7F3E8`), and — the detail that actually makes a two-theme system read as *one* considered design instead of an automatic inversion — the two modes deliberately mirror rather than simply invert each other: dark mode's `background` (`#0D1512`) and light mode's `ink` (`#14201B`) sit in the same deep charcoal-forest family, and light mode's `background` is close to dark mode's `ink` (`#F6F1E7`, warm champagne). `onAccent` stays dark-on-accent in both modes (same reasoning as before: the emerald is light/saturated enough in both palettes that white text on it would be a contrast problem). Same values-only change, same zero-consumer-edits property, same snapshot set regenerated again on top of the first pass's diffs, same 226/227 result (only the pre-existing, unrelated `date-time-sheets` failure).
- **Added depth to `AppPalette` (gradient + sheen tokens) and rebuilt `app/index.tsx` to match `stitch/landing/vaya-landing-premium-dark-mode.html`'s structure directly, on explicit feedback that the flat-hex palette still "looked flat."** A solid-fill token set is premium-*adjacent* at best — real depth in apps like this comes from gradient and light, not color choice alone — so `AppPalette` gained five new required fields alongside the existing flat roles: `backgroundGradient`/`surfaceGradient`/`accentGradient`/`inkGradient` (two-stop `expo-linear-gradient` tuples, each a richer rendering of an existing flat role — `background`, `surface`, `accent`, `ink` — not a parallel palette) and `glimmer` (a translucent highlight for a diagonal sheen swept across a card or button, the detail that makes a gradient fill actually catch light instead of just being a smoother flat fill). Confirmed via grep that `lightPalette`/`darkPalette` are the only two `AppPalette` object literals anywhere in the codebase, so adding required fields was safe with zero other call sites to update. Scoped deliberately: this pass enriches the *token set*, not every existing hand-rolled screen CTA — per this worktree's established precedent (driver/publish/onboarding screens hand-roll their own themed chrome rather than share a `Button` primitive), retrofitting every already-shipped flat `theme.ink`/`theme.accent` fill this session built is separate follow-up work, not bundled here. The landing screen is this pass's real showcase and gets the full treatment: a `LinearGradient` background wash (`backgroundGradient`, replacing the flat `darkPalette.background` fill) for a vignette instead of a flat plane, and the primary CTA is a gradient (`inkGradient`) button with a diagonal `glimmer` sheen layered on top (a second, rotated `LinearGradient` clipped to the button, `transparent → glimmer → transparent`) rather than a flat matte fill. Also restructured the screen itself to match the Stitch reference's actual layout rather than the previous pass's centered-hero guess: "VAYA" wordmark pinned to the header (was stacked mid-hero), content bottom-anchored via a flex spacer (was vertically centered), and — reference-accurate rather than the earlier two-step guess — the real phone-entry mechanism now sits directly inside a `GlassSurface` card on the landing screen itself, matching how the reference shows its Google/Facebook/email options inline in one glass card with no intermediate screen, instead of being hidden behind a second `BottomSheet` tap (that companion-sheet pattern was this session's own earlier design choice, made before this specific reference file was the stated target; it's dropped now that fidelity to this exact screen is the ask). Same real-mechanism divergence as before: no OAuth stand-ins, the one real phone/OTP mechanism this backend has sits where Stitch's three buttons are. Verified: design-system + mobile typecheck/lint clean; full test suites re-run with no snapshot impact (the landing screen has no snapshot test) — design-system 83/85 (same pre-existing `date-time-sheets` failure), mobile 141/141.
- **Fixed the landing screen's phone-entry keyboard covering the input/CTA, and added tap-outside-to-dismiss, matching the exact pattern `(auth)/otp.tsx` already establishes.** The `KeyboardAvoidingView` from the pass above was scoped to just the glass card's inner content, which does nothing useful — the outer screen layout (the flex spacer that bottom-anchors the hero+card) never responded to the keyboard at all, so on a real device the keyboard would sit on top of the phone field and "Continuer" button rather than the screen shifting to reveal them. Moved to wrap the *entire* screen instead (`behavior: Platform.OS === 'ios' ? 'padding' : undefined`, identical to `otp.tsx`/`driver/onboarding/vehicle.tsx`'s existing convention) so the bottom-anchoring spacer shrinks and the card rises above the keyboard instead of being covered by it. Also wrapped the screen in a `TouchableWithoutFeedback`/`Keyboard.dismiss` pair (same `accessible={false}` pattern `otp.tsx` uses) so tapping anywhere outside the phone field or CTA dismisses the keyboard — nested touchables (the `TextInput`, the CTA `TouchableOpacity`) still receive their own taps first, per RN's normal hit-testing, so this doesn't interfere with actually using the form. Verified: mobile typecheck/lint clean, 141/141 tests passing (no snapshot test covers this screen).
- **Added a route-selection step to ride creation, and made candidate-stop generation trip-length-aware** (full design: `docs/domain/ride-engine.md`'s new "Route-selection step" section). Real gap this closes: `createRide` always silently computed one default route and generated stop candidates against it — a driver had no way to see or choose between the fastest route, a toll-avoiding route, or a highway-avoiding one, even though a market like Tunisia's genuinely has meaningfully different real road options (péage vs. route nationale) for many intercity corridors. `RoutingProvider` (`apps/api/src/lib/routing-providers/`) gained `computeRouteAlternatives` on both adapters — Google Routes fires the default `computeAlternativeRoutes` request plus explicit `avoidTolls`/`avoidHighways` requests in parallel, deduplicating any option whose geometry doesn't actually differ from one already found; OSRM uses its own `alternatives=true`, honestly labeled `fastest`/`alternative` only (no toll/highway modifier vocabulary exists for that path — `hasTolls` stays `null`, never guessed). New `apps/api/src/modules/rides/route-options.service.ts` (`POST /rides/route-options`, stateless — no ride exists yet) mints a short-lived, one-shot Redis token per option; `createRide` redeems it (`routeToken`, optional on `createRideSchema`) for the exact server-computed route rather than trusting any client-supplied distance/geometry — the same server-authoritative discipline Phase 6's pricing bounds already established, extended to route choice. `rides.routeKind` (migration `0016_add_ride_route_kind.sql`, additive/nullable) records which kind was picked. Mobile: a new `'route'` step in `(tabs)/publish.tsx`'s wizard, between the origin/destination form and pickup/dropoff selection — all alternatives drawn on one persistent map at once (Google/Waze route-picker convention: unselected muted, selected solid + on top) with a new `RouteOptionCard` design-system primitive underneath; shown only when there are 2+ genuinely distinct options, skipped entirely otherwise (a network hiccup or a route with only one real answer degrades straight to the pre-feature behavior, never blocking publish). Separately, `stop-candidates.service.ts`'s fixed 1km/8-candidate/150m-merge defaults are now derived per-route from a new pure `packages/domain/src/route/classify-trip-profile.ts` (`commute` ≤15km: denser 500m sampling, tighter 120m merge; `urban` ≤45km: the original defaults, unchanged; `intercity` >45km: sparser 2.5km sampling but a higher 12-candidate cap) — a short commute and a long intercity haul now get differently-tuned stop suggestions instead of one fixed density for every trip length. **Genuinely verified, not assumed:** `pnpm typecheck`/`lint` clean across `domain`/`validation`/`api`/`design-system`/`mobile`; new unit tests for `classifyTripProfile` (domain) and a new `route-options.integration.test.ts` against this session's real docker-composed Postgres/Redis (token mint/redeem/one-shot/origin-mismatch-rejection, all real Redis round-trips); full existing suites re-run with no regressions (api 167/171 passing — the 4 failures are pre-existing and unrelated: 3 need real Google OAuth credentials this sandbox doesn't have, 1 is `matching-tiers.integration.test.ts`'s date-dependent `closest_departure` fixture, confirmed unaffected since `matching.service.ts` was never touched by this change; domain 68/68, validation 6/6, design-system 110/110, mobile 223/223). **Not verified against live infrastructure**: no real `GOOGLE_MAPS_SERVER_API_KEY`/`GOOGLE_ROUTES_API_KEY` and no real Tunisia OSRM routing graph were available in this sandboxed environment, so `computeRouteAlternatives`'s toll/highway-avoidance behavior (both providers) and the mobile route-picker's on-device rendering are unverified against real infrastructure — the Redis token mechanism, the graceful single-option/failure fallback, and every pure scoring/classification function are the parts this session could and did verify for real.
- **Fixed a real layout bug in the driver's "Request details" sheet (`RequestDetailSheet.tsx`) and closed two gaps in the same flow.** The Accepter/Refuser action bar sat flush against the sheet's own bottom edge with no device-safe-area awareness, so it rendered underneath the iOS home indicator / Android gesture bar on any device with one — confirmed live from a real device screenshot. `BottomSheet` (`packages/design-system`) gained an optional `bottomInset` prop (mirroring `Toast`'s existing `topInset` precedent — this package still never imports `react-native-safe-area-context` itself), applied only when a caller passes it (a bare style-array shape change would have rippled into every other `BottomSheet` consumer's snapshot for nothing), wired from `RequestDetailSheet` via `useSafeAreaInsets().bottom`. Also: the sheet's compact 120px map preview is now tappable/expandable into a real fullscreen `MapCanvas` showing the driver's whole real route (muted, thin) with this specific request's segment overlaid on top (accent, thick) plus a two-line legend and pickup/dropoff pins — previously there was no way to see the request against the driver's actual full route at all, just the isolated segment. `driver/rides/[rideId].tsx`'s `PendingRequestRow` card is now entirely tappable to open request details (was only the small info icon — the whole point of the ask, verified against a real screenshot showing the icon as the only tap target), with Accept/Decline kept as their own nested `TouchableOpacity`s (RN's standard nested-touch responder behavior keeps them independently tappable). `search/trust.tsx` (the shared "view someone's profile" screen, reached both rider→driver and now driver→passenger via a new `bookingId` param) had every stat/pill/tier section gated on `profile.driver` alone, so a passenger's own profile rendered as nothing but an avatar and a first name — it now reads whichever of `trustSummary.driver`/`trustSummary.rider` actually applies (`GET /users/:id/trust-summary` already computes both TierAggregates for every user), while KYC verification/bio/vehicle correctly stay driver-only (never fabricated for a rider who never onboards). The "Message" button is real, not just re-labeled: `useGetConversationByBookingQuery(bookingId)` checks for an actual Phase-8 conversation (created only once the booking reaches `accepted`) and only enables the button once one genuinely exists, with an honest disabled state and role-aware copy otherwise. The screen's hardcoded "Vaya" header title is replaced with a generic `search:trust.title` ("Profile"/"Profil"/"الملف الشخصي"). Verified: `pnpm typecheck`/`lint` clean across `design-system`/`mobile`; new/updated snapshot coverage (`search-trust-screen`, `driver-ride-hub-screen`) for the rider-trust-stats case, the enabled-Message case, and the tappable-card case; full mobile/design-system suites re-run with no new regressions (design-system 122/122; mobile's 10 pre-existing failures — locale/ICU date-format drift in `search-results-screen`/`profile-screen`/`booking-detail-screen`, none of them touched by this change — reproduced identically on a clean `HEAD` checkout, confirmed not caused by this pass). **Not verified**: on-device rendering of the fullscreen map overlay and the safe-area padding fix, same sandboxed-environment limitation as every other on-device item in this file.
- **Made every real user's avatar photo across the app a working tap target into their profile (`search/trust.tsx`), on explicit direction — most were previously decorative.** Audited all 13 files rendering `<Avatar>`; wired 9 real gaps: the Messages inbox row, a rider's trip-card driver avatar (`(tabs)/trips.tsx`, needed a new `CardCounterpart.userId` field — `booking.ride.driverUserId` was already fetched, just never threaded to the card), the booking-detail screen's `DriverCard`, the conversation header avatar, the driver ride-hub's pending-request AND already-answered rows (nested `TouchableOpacity`s independent of each row's own card-level tap target, same pattern `RequestDetailSheet` already established), `DriverBookingDetailSheet`'s identity row, the post-booking `bookings/pickup.tsx` screen (turned out to need zero new plumbing — `driverUserId` was already flowing wholesale through the untyped `params` object all the way from `ride-details.tsx`'s `requestSeat()`, just never declared in `pickup.tsx`'s own `useLocalSearchParams<T>()` type or read), and the search-results driver card (`DriverListCard` gained a new optional `onAvatarPress`/`avatarAccessibilityLabel` pair — additive, zero behavior change for the existing single-`onPress`-on-the-whole-card contract, so no other consumer needed touching). Left three deliberately, not missed: `search/trust.tsx`'s and `search/reviews.tsx`'s own avatars (self-referential — already on that profile), `(tabs)/profile.tsx`'s (the signed-in user's own), and — the one with an actual reason — `search/ride-details.tsx`'s fellow-passenger list, where `useListFellowPassengersQuery`'s own doc comment is explicit that a fellow passenger's identity is "public, first-name-only... never a full identity," a real backend privacy boundary; `GET /users/:id/public-profile` always returns a full legal name, so wiring that avatar to trust.tsx would leak exactly what that endpoint deliberately withholds (left a code comment explaining this, not a silent omission). `notifications/index.tsx` was already correctly wired before this pass. Added a new shared `common:actions.viewProfile` i18n key (en/fr/ar) rather than duplicating the existing driver-specific/notification-specific variants. Verified: `pnpm typecheck`/`lint` clean across `design-system`/`mobile`; full suites re-run — design-system 122/122, mobile 257/262 (the 5 remaining failures are the pre-existing, unrelated `profile-screen` locale/ICU date-format drift, confirmed by an untouched `git diff` on that file); several snapshot suites this pass touched (`booking-detail-screen`, `driver-ride-hub-screen`, `messages-screen`, `search-results-screen`) needed regenerating for the new nested-`TouchableOpacity` structure, reviewed diff-by-diff to confirm only the intended avatar-wrap change (plus, incidentally, fixing that same pre-existing date-format drift in `search-results-screen` for free). **Not verified**: on-device tap behavior of the nested-Touchable pattern, same sandboxed-environment limitation as every other on-device item in this file — the RN responder-negotiation behavior it relies on is standard and used elsewhere in this codebase already (`PendingRequestRow`'s own Accept/Decline buttons), not a new assumption.

- **Rebuilt the driver ride-hub's itinerary as a threaded timeline that makes overlapping passengers legible, on explicit direction against a UI-redesign spec for the "My trip" screen.** Real gap this closes: `driver/rides/[rideId].tsx`'s `passengerItineraryPoints` was already real, route-order-sorted data (every accepted passenger's own pickup/dropoff, correctly interleaved between the ride's origin/destination — Phase 13/M-004 work) but rendered as a flat list of identical-looking rows, so a ride with two overlapping passengers (one picked up before the other is dropped off) showed no signal the car is ever carrying both at once — exactly the ambiguity the reference screens were flagging. New pure `apps/mobile/src/features/driver-rides/itineraryThread.ts` (`buildItineraryThread`, unit-tested, no React/network) turns that same sorted point list into an origin→events→destination node thread plus a real per-leg onboard-seat count (summed from each booking's actual `seatsRequested`, not a passenger headcount), and flags the exact node where a second passenger boards while the first is still aboard. New `apps/mobile/src/features/driver-rides/PassengerItineraryThread.tsx` renders it: one connector per leg — dashed/`outline` when empty, solid `accent` with one passenger aboard, solid and heavier `accentStrong` with two or more — plus an inline occupancy tag on the exact overlapping pickup. The map preview and the fullscreen route modal now draw the same real route geometry (`utils/polyline.ts`'s existing `sliceRouteBetween`) split into those same colored/dashed legs instead of one flat line, via a new `MapPreview` `occupancySegments` prop (backward-compatible — every other caller is unaffected). `PassengerStopPin` (`packages/design-system`) now colors pickup `accent`/`+` vs. dropoff `error`/`−` instead of one uniform color for both, so the map and the itinerary read as the same real point with the same real meaning; this needed a new `AppPalette.onError` token (a solid-`error`-filled shape had no contrast-safe foreground color in either theme before now — `error` flips lightness between light/dark mode the same way `accent`/`ink` already do). The map card also gained a compact two-stat capacity/earnings bar (real seat-by-seat fill pips, confirmed amount next to what the ride could still earn), replacing 3 stacked icon+text fact rows. **Genuinely verified, not assumed:** new unit tests for `buildItineraryThread` (6 cases: single passenger, a real crossing-overlap case, multi-seat bookings summing correctly, sort-by-route-order, the no-passengers case) and for `MapPreview`'s new segment coloring/fallback (design-system); the driver-ride-hub screen's existing snapshot suite extended with a real two-overlapping-passengers fixture and regenerated; `pnpm typecheck`/`lint` clean across `design-system`/`mobile`; full suites re-run with no new regressions (design-system 124/125 — the 1 failure is the pre-existing, unrelated `date-time-sheets` `GestureHandlerRootView` nesting issue; mobile 259/269 — the 10 failures are the pre-existing, unrelated `profile-screen`/`search-results-screen`/`booking-detail-screen` locale/ICU date-format drift, confirmed identical on a clean `HEAD` checkout). **Not verified**: on-device rendering of the dashed/colored polylines and the threaded timeline's real layout, same sandboxed-environment limitation as every other on-device item in this file.

**Journey-contract TDD initiative (`docs/unified_driver_and_passenger_journey.md` vs. the live codebase, separate from the numbered roadmap; full detail: `docs/tdd_journey_test_matrix.md` + `docs/tdd_journey_test_report.md`):** a TDD-first pass decomposed the newer unified spec into ~100 individually testable requirements (`M-001`…`M-125`, `EDGE-046`…`EDGE-056`, `INV-01`…`INV-09`, vertical journeys `V-01`…`V-10`), wrote the real tests first (mostly RED against the codebase as it stood), then implemented against that gate. Landed so far, across this initiative's several sessions: booking-acceptance/request-deadline mechanics (`bookings.expiresAt`, cross-ride "same journey" grouping/capping/superseding — M-051–058), the VAYA Operational Policy Configuration admin UI (`apps/admin/src/pages/OperationalConfigPage.tsx`, the first and only place matching/cancellation/no-show/deadline thresholds can be changed without a code deploy), Layer-A trip-lifecycle inference wired into the real trip flow (auto-start/boarding/route-deviation), in-progress-ride live-position matching (M-091), real pickup/dropoff resolution against the passenger's actual point instead of trusting a stop id verbatim (M-004/M-020, `bookings.pickupWalkMeters`/`dropoffWalkMeters`), a genuine joint passenger-walk/driver-detour optimization for recommended stops instead of the two previously-disconnected single-objective passes (M-039, `packages/domain/src/matching/joint-stop-score.ts`), pedestrian-zone and vehicle-stop-access rejection for both driver-published and passenger-facing stops (M-014/M-015, plus a real M-017 bypass gap closed along the way — `addCustomStop`'s pickup/dropoff branch had zero road-validation at all before this), and a passenger pickup/dropoff override mechanism with a real pre-commit recalculation preview (M-040/EDGE-053, a new `GET /rides/:rideId/pickup-override-preview` plus a real long-press-to-place-a-point affordance on `search/pickup-point.tsx`). Also closed, found along the way rather than assumed complete: M-054's backend field had shipped with no real mobile caller anywhere (`bookings/confirmed.tsx` was still running its own fixed-7-minute client-only countdown with its own "not a real deadline" comment; the driver-facing request views showed no deadline at all) — now shown for real everywhere the spec names. **Vertical-journey suite (`tests/e2e/tests/journeys/`), resolved 2026-08-29**: real `GOOGLE_MAPS_SERVER_API_KEY`/`GOOGLE_ROUTES_API_KEY`/`GOOGLE_PLACES_API_KEY`/`GOOGLE_GEOCODING_API_KEY` values (found in a sibling worktree, `worktree-gmp-verification`) were configured in `apps/api/.env` — `ROUTING_PROVIDER=auto` picks Google whenever a server key is present, so a live OSRM instance (this repo's docker-composed OSRM still has no Tunisia extract prepared, unrelated to this fix) is no longer required. All 10 journeys, 12 cases, now pass end-to-end against a real server. Doing this surfaced and led to fixing two real bugs the previous OSRM-down fallback path had been silently masking: `matching.service.ts`'s in-progress-ride matching (M-091) required a driver-selected stop at both the pickup AND dropoff end, but the spec's own worked example needs no stop at a destination that's simply the ride's own unchanged endpoint; and `bookings.routes.ts`'s no-show report body schema rejected a genuinely empty POST body (Fastify parses it as `null`, not `undefined`, which Zod's `.default({})` doesn't cover). Full accounting: `docs/tdd_journey_test_report.md` §13. **Full spec closure, 2026-08-30 (`docs/tdd_journey_test_report.md` §14)**: on explicit direction that full spec compliance (not just the items named above) is the bar, every requirement this matrix still listed genuinely open was closed with a real, executed test — M-085/M-085a (admin-injectable `maxDetourRatio`), M-083/M-084/EDGE-052/INV-09 (existing-passenger soft protection, wired into `createBooking` for real), M-081/M-082 (segment-aware search capacity across every matching tier — the most invasive fix, which also surfaced and fixed two further real bugs: a hardcoded `seats_available > 0` re-baked into all three PostGIS candidate pre-filters, and an unordered `LIMIT` in `findCandidateRideIdsByCorridor` that could silently drop a rider's own best match), M-104 (automatic no-show classification — new `packages/domain/src/trip/no-show-inference.ts`, wired into the existing trip-staleness sweep), and M-113 (the 4 structurally-missing notification event types — `booking_deadline_approaching`, `booking_sibling_cancelled`, `trip_active`, `trip_eta_changed` — each now a real, dispatched event with specific copy). M-092 was re-examined rather than force-closed: its "existing-passenger impact" gap turned out not to be a real gap for that specific tier (every in-progress-tier candidate resolves through a stop-based booking path, never a genuine free-form detour insertion, so there is structurally nothing for that check to evaluate there). As of this pass, no row in the matrix reads a plain `FAIL` — remaining `PARTIAL`s are honestly scoped to either a real environment limitation (no on-device UI verification, no prepared Tunisia OSRM graph) or a genuinely subjective visual/UX bar needing a running app to judge, not more backend code. Read `docs/tdd_journey_test_matrix.md` before touching matching, pricing, stop generation, booking creation, or the passenger pickup/dropoff flow — it is the current, line-by-line source of truth for what's actually implemented in this area, superseding this file's own phase-level summaries above wherever the two disagree.

**Google Maps Platform + PostGIS initiative (separate from the numbered roadmap above, user-directed; Phases 1-4 of 8 done — full detail: `docs/product/google-maps-platform-implementation-report-2026-08-23.md`):** replaces Nominatim/OSRM as the *primary* location/routing provider with Google Places API (New) + Routes API, behind a new `LocationProvider`/`RoutingProvider` abstraction (`apps/api/src/modules/geocoding/providers/`, `apps/api/src/lib/routing-providers/`) — Nominatim/OSRM are kept, unchanged, as the automatic fallback whenever no Google key is configured (`LOCATION_PROVIDER`/`ROUTING_PROVIDER=auto`), so every environment without a real key behaves exactly as before this change. Adds a PostGIS spatial layer (migration `0014`, additive `geography(Point/LineString,4326)` columns generated from the existing lat/lng columns — zero dual-write) that `matching.service.ts`'s tiers now query as a stage-1 cheap candidate filter before the existing scoring logic runs, replacing the prior full time-windowed table scan (falls back to that exact scan, unchanged, whenever PostGIS is disabled or a query fails). Adds a real, routing-engine-calculated `detour_match` tier — the first genuine answer to the "detour matching" gap both search-engine audits flagged as the highest-leverage missing capability — gated by a hard-capped (15 candidates), PostGIS-pre-filtered set of real multi-waypoint routing calls, never an uncapped N-candidates-×-N-routing-calls pattern; every detour candidate is `pickupViable`/`dropoffViable: false` by design (surfaces a real, calculated possibility, never a bookable stop — turning it into one needs a driver-confirmation capability this pass deliberately doesn't build). Mobile: `search/composer.tsx` now uses the real Places API (New) session-token autocomplete+details flow (was a single Nominatim call) with type-labeled result rows (city/governorate/neighborhood/poi/address — fixes a real "Sousse / Ville · Sousse Governorate / Sousse / Gouvernorat" duplicated-label problem); every map in the app (`MapCanvas`, the one shared primitive every screen's map renders through) now explicitly requests Google Maps SDK (`PROVIDER_GOOGLE`, was `PROVIDER_DEFAULT`) on both platforms, with a new Expo config plugin wiring the iOS native key. **Not verified against live infrastructure**: no Docker/Postgres/PostGIS/OSRM instance and no real Google API key were available in the implementation session's sandboxed environment — every typecheck/lint/pure-unit-test suite passes (reproduced in the implementation report), but the PostGIS migration, the Google API calls, and any on-device mobile behavior are unverified against real infrastructure, not merely untested. **Phases 5-8 status, corrected 2026-08-28**: passenger/driver detour-UX polish beyond one results-list badge is still open. The active-trip/live-GPS-journey system — flagged by `docs/product/search-engine-audit-v2-active-trip-2026-08-23.md` as having zero existing infrastructure — **has since been built** in later commits (driver GPS broadcast, WebSocket/Redis live tracking, automatic GPS-proximity lifecycle transitions, BullMQ staleness-sweep auto-completion) and is genuinely real, not mock. This file previously carried the stale "zero infrastructure" claim after that work landed; see `docs/product/unified-journey-audit-2026-08-28.md` for the verified current state, including the two real gaps it found in this area (live tracking isn't wired into matching/search, and pre-boarding GPS exposure doesn't yet match the intended passenger-privacy tiering).

## Known gaps and deferred manual steps

Distinct from "Important decisions" below (those need a product/business call; these need an environment/infrastructure action or UI work that's straightforward but simply hasn't been built). Carried forward honestly rather than marked done — per this file's own "never claim fabricated success" rule.

- ~~No driver-side per-booking screen~~ — **closed.** `(tabs)/trips.tsx`'s driver dashboard already had `RideRequestsSheet` (accept/decline, with rider identity) and `ManageRideSheet` (whole-ride cancel) from earlier undocumented work; what was still missing — message/cancel/report-no-show for one already-*accepted* booking — is now `DriverBookingDetailSheet` (`apps/mobile/src/features/driver-rides/`), reached by tapping an accepted row in `RideRequestsSheet`'s "Déjà traitées" list. Reuses `CancellationSheet`/`NoShowReportSheet` exactly as-is (both were already `role`-aware — just never passed `role="driver"` from any real screen before this) and the same `conversations/[bookingId].tsx` screen the rider side uses (one conversation per booking, not per role, so nothing driver-specific was needed there). No backend changes — every endpoint already supported either party.
- **Android Google Maps API key not set.** `GOOGLE_MAPS_API_KEY` is empty in `apps/mobile/.env.example` and no `.env` exists in this worktree. `app.config.js` wires the env var into Android's native Google Maps config correctly, but without a real key `react-native-maps` renders a blank gray tile area on Android (app doesn't crash; iOS is unaffected since it uses Apple Maps by default). Needs a real key from Google Cloud Console plus a native Android rebuild (not hot-reloadable via Expo Go).
- **No live push-notification delivery verified.** Phase 7's dispatch path (BullMQ → Expo push HTTP call) is fully implemented and tested against a mocked Expo response, but this environment has no real EAS project id / APNs / FCM credentials — nothing has ever actually been pushed to a physical device.
- **No on-device map/UI verification at all.** Every phase's manual "verify on a real device/simulator" DoD item was necessarily skipped in this sandboxed environment — automated tests pass, but nothing here substitutes for actually running the Expo app.
- **iOS App Transport Security exception added for the http-only dev API — pending a real device rebuild to confirm.** Real bug found live: uploaded profile photos rendered correctly on Android but silently fell back to initials on iOS (`Avatar`'s `onError` fallback catching a real image-load failure). Root cause: the API has no HTTPS backend yet (`http://localhost:3000` / a LAN IP in every dev setup this file documents), and iOS's App Transport Security blocks any non-https load by default outside Expo Go's own permissive Info.plist — so a real iOS build/dev-client (unlike Expo Go, and unlike Android, which has no equivalent restriction here) blocks the avatar image request outright. Fixed via `ios.infoPlist.NSAppTransportSecurity.NSAllowsArbitraryLoads: true` in `app.config.js`, matching this file's other native-config entries (needs a real prebuild/rebuild to take effect, not hot-reloadable) — unverified against a real iOS build in this sandboxed environment, same limitation as the Android Maps key above. Revisit (tighten to a scoped exception domain, or drop entirely) once the API has a real HTTPS endpoint.
- **Messaging has no moderation/reporting.** Open Decision #6 (below) — deliberately out of Phase 8's scope, still genuinely unbuilt.

## Important decisions

See `docs/roadmap/README.md`'s Open Decisions section for the full list requiring a human call (confirming the Phase 6 first-cut `base_rate_per_km`, platform fee activation, messaging moderation). Open Decision #4 (cancellation policy without a payment system) is now resolved — see `docs/domain/cancellation-policy.md`. Open Decision #5 (dark mode) is now resolved, scoped to the rider search + booking-request flow — see `docs/design-system/README.md`'s Colors section. The "arc." vs "VAYA" landing-screen naming flag from the original audit is now resolved too: `app/index.tsx` was rebuilt against the Stitch "Vaya Landing" project (see the Post-Phase-11 fixes entry below) and uses "VAYA" throughout, matching every other screen in the app — "arc." no longer appears anywhere.

## Things that must NOT be changed casually

- The authoritative state-machine location (`packages/domain` for ride/booking status transitions) — do not reimplement transition logic elsewhere even "just for one screen."
- The OSRM-based routing foundation and its haversine fallback pattern (`lib/routing.ts`) — this is real, working infrastructure; don't replace it with a different routing approach without a documented reason.
- The 3-tone brand color palette (`packages/design-system/src/tokens/colors.ts`) — this is the strongest, most distinctive asset in the current product; don't dilute it toward generic defaults.
- `bookings`/`rides`/`trips` schema's existing columns, once the ride-engine/pricing migrations land — additive changes only (new nullable FKs, new tables), per the explicit backward-compatibility rollout notes in `docs/domain/ride-engine.md` and `docs/domain/pricing.md`. Do not force a hard cutover that breaks rides published before a phase ships.
