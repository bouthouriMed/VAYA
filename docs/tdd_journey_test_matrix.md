# VAYA Journey Contract — Requirement Extraction Matrix

**Status:** temporary working document for the TDD-first phase. Source of truth for *what VAYA must do* remains `docs/unified_driver_and_passenger_journey.md` ("the spec"). This matrix decomposes it into individually testable requirements and maps each to the executable test(s) that encode it, plus the requirement's *current* status as of `main`@`8b61f21`/`2019ed7`, per `docs/product/unified-journey-audit-2026-08-28.md` (\"the audit\").

**Current result** legend: `PASS` = behavior already correct, test expected green. `FAIL (missing)` = capability doesn't exist. `FAIL (incorrect)` = capability exists but contradicts spec. `FAIL (partial)` = foundation exists, behavior diverges. Where the audit didn't cover a requirement directly, current result is inferred and marked `(inferred)`.

Test ID scheme: `<layer>.<area>.<slug>` — `A.` = domain/unit (Layer A), `B.` = API/integration (Layer B), `C.` = mobile E2E (Layer C), `V.` = vertical journey (spans layers). Layer A tests live under `packages/domain/src/**/__tests__/*.contract.test.ts`. **Vertical journeys (V-01..V-10) live under `tests/e2e/tests/journeys/*.api.test.ts`** — real Playwright HTTP calls against a live server + real Postgres/Redis, following the pre-existing `search-to-booking.api.test.ts` convention, NOT the originally-planned `tests/contract/*.integration.test.ts` location: this reuses `tests/e2e`'s already-working Playwright harness immediately, and — per explicit direction this session — a vertical journey must verify the actual USER EXPERIENCE (real HTTP requests exactly as the mobile app's RTK Query client makes them) rather than calling internal service functions directly. Shared helpers live in `tests/e2e/tests/support/journey-helpers.ts`.

**Test layer taxonomy (clarified this review pass):** this matrix's `A.`/`B.`/`C.`/`V.` prefixes map directly onto the mission's own "domain/unit contracts" vs. "vertical integration contracts" distinction. **Domain/unit (Layer A)** — route overlap, route projection, detour calculation, ETA confidence, candidate scoring, segment pricing, segment capacity, cancellation policy, state transitions, boarding/auto-start inference, no-show evaluation — pure functions, no I/O, no DB, exercising exactly one `packages/domain` concept per file. **Vertical integration (Layer B and V.)** — exercise the real application stack together (`API → services → matching → database → booking → notifications/events`), never stopping at an isolated pure function; `V-01..V-10` are the canonical end-to-end proof of this, real HTTP calls through the live Fastify server against real Postgres/Redis. A requirement whose test column lists only an `A.` ID and no `B.`/`V.` ID is, by construction, unverified at the integration level even where the pure logic is proven — flagged per-row above where that gap is real (e.g. M-070's formula is `A.`-proven but its wiring is `B.`-FAIL; M-101's ride-level half has no `A.` predicate to call yet at all).

---

## Section 1–2: Core model & product principles

| ID | Spec ref | Behavioral requirement | Test(s) | Current result |
|---|---|---|---|---|
| M-001 | §1, P1 | Search is not a bare filter — must find best solution to passenger's requested journey, expanding intelligently when no exact match exists | `B.search.cascade-fallback`, `V.journey-2-mid-route` | PASS (cascade exists) |
| M-002 | §1, P3 | Passenger route may partially overlap driver route; system calculates feasibility, not exact-equality | `B.search.partial-overlap-zaragoza-barcelona` | PASS |
| M-003 | P2 | Driver publishing Madrid→Barcelona automatically yields derived segments (Madrid→Zaragoza, Zaragoza→Barcelona, etc.) without manual publication | `B.search.derived-segments-not-manually-published` | PASS |
| M-004 | P4 | Passenger's requested coordinates are not automatically the physical meeting point — VAYA determines practical pickup/dropoff | `A.stops.corridor-intent-not-fixed-coordinate`, `B.booking.pickup-resolved-not-passthrough` | **PASS, implemented and verified live this pass.** `bookings.service.ts`'s new `resolveStopWalkMeters` independently re-derives the real walk distance from the passenger's own `requestedPickup`/`requestedDropoff` to the chosen stop and rejects a selection implausibly far from it (400) rather than trusting the client's `pickupStopId` verbatim; the real distance is persisted (`bookings.pickupWalkMeters`/`dropoffWalkMeters`, migration `0024`). Wired into the real mobile client this pass too (`ride-details.tsx` now sends `requestedPickup`/`requestedDropoff` — previously computed server-side but never actually sent by any real caller). Verified: `bookings-pickup-resolution.integration.test.ts`, 3/3 real HTTP via `app.inject` against real Postgres. |
| M-005 | P5 | Driver and passenger detail views expose different, role-appropriate framings of the same match (driver: impact-on-my-trip; passenger: can-this-get-me-there) | `B.request-detail.driver-view-shape`, `B.search-result.passenger-view-shape` | PASS (partial — see M-0xx deadline gaps) |
| M-006 | P6 | Lifecycle buttons (Start trip / Passenger onboard / Finish / Report no-show) are confirmations, not sole state source; VAYA infers automatically where reliable signals exist | `A.trip-lifecycle.auto-start-without-button`, `A.trip-lifecycle.auto-board-without-button`, `A.trip-lifecycle.auto-complete-without-button` | **PASS — doc corrected, was stale. See M-099/M-100 and M-096/M-097.** Completion, start, and board are now all auto-inferred; report-no-show remains a real-report action (see M-104's own MAY-not-MUST framing). |
| M-007 | P7 | ETAs carry confidence classification (estimated/confirmed/inferred/unavailable); never claim onboard from momentary GPS proximity | `A.gps.no-fabricated-certainty`, `A.boarding.momentary-proximity-insufficient` | **PASS — doc corrected, was stale.** `classifyEtaConfidence` (implemented in an earlier session on this branch) is now surfaced on `getTrackingState`. The "never claim onboard from momentary proximity" half is M-096's own sustained-proximity gate — see that row. |
| M-008 | P8 | Map communicates planned route, passenger route, overlap, pickup, dropoff, detour, live progress; viewport intelligently fitted | `C.trip-detail.map-elements-present` (Layer C, presentational — behavioral proxy only) | (inferred) PARTIAL |

## Section 3–6: Driver publishing (origin/destination/route/pickup/stops/publish)

| ID | Spec ref | Behavioral requirement | Test(s) | Current result |
|---|---|---|---|---|
| M-010 | §3.1 | Driver can search origin/destination by city, street, landmark, arbitrary place — not city-constrained | `C.publish.origin-search-arbitrary-place` | PASS (Google Places integration) |
| M-011 | §4 | Route alternatives are computed, distinguishable, and one is selected before pickup config | `B.publish.route-alternatives-computed`, `A.route.classify-trip-profile-regression` | PASS (route-options.service) |
| M-012 | §4 | Selected route is the one used by all subsequent matching/ETA (not silently re-derived) | `B.publish.selected-route-token-redeemed-verbatim` | PASS (routeToken mechanism) |
| M-013 | §4.1 | Recommended pickup points near origin must not be on a highway | `A.stop-candidates.reject-highway-speed-samples` | PASS (speed-inferred reject) |
| M-014 | §4.1 | Recommended pickup points must not be in pedestrian-only areas | `A.stop-candidates.reject-pedestrian-zone` | **PASS, implemented this pass.** New `isPedestrianOnlyLocation` (stop-candidates.service.ts) — a conservative, allowlist-based classifier over real OSM `class`/`type` tags (highway=pedestrian/footway/path/steps/cycleway/bridleway, leisure=park/garden/playground, place=square, natural=*, landuse=forest/meadow/...), real only for Nominatim (Google carries no equivalent tag — honestly null there, never guessed). Wired into `scoreStopCandidate` as a new `pedestrian_zone` hard-reject reason, and into `addCustomStop` for a driver-placed point (a real reverse-geocode check, best-effort — never hard-fails on a lookup failure). Verified: 8 new unit tests + `scoreStopCandidate` cases (`stop-candidates.service.test.ts`, 47/47 total). |
| M-015 | §4.1 | Recommended pickup points must not be operationally unsuitable / vehicle cannot stop | `A.stop-candidates.reject-no-stopping-feasibility` | **PASS, implemented this pass.** New `exceedsStopAccessDistance`/`MAX_STOP_ACCESS_METERS` (2000m) — real, live evidence from OSRM's own `nearestRoad` snap distance (a point genuinely ~2km from any drivable road cannot have a vehicle stop near it). New `no_stopping_feasibility` reject reason in `scoreStopCandidate` (free for route-sampled candidates — `snapDistanceM` was already computed for deviation cost) and in `addCustomStop` (real hard rejection replacing the old silent-accept-unsnapped behavior beyond the same threshold). Verified: unit tests + 2 new live integration tests (`stop-candidates.integration.test.ts`: pickup-role snapping, open-water rejection). |
| M-016 | §4.1 | Driver can select a recommended point OR manually place another point | `B.publish.manual-pickup-point-accepted` | PASS (mechanism exists) |
| M-017 | §4.1 (implicit invariant) | A manually-placed point is still subject to the same feasibility validation as a recommended one — no bypass | `B.publish.manual-point-rejects-highway` | **PASS, fixed this pass.** `addCustomStop`'s pickup/dropoff branch previously performed zero road-snapping/validation at all (confirmed live before the fix). All three roles (pickup/dropoff/via) now go through the same pedestrian-zone + `nearestRoad` snap/access-distance checks — real, executed integration test (`M-017: a manually-placed pickup point is snapped/validated exactly like a via stop, not bypassed`) confirms a pickup point now comes back road-snapped like a via stop. |
| M-018 | §4.2 | Same accessibility rules apply symmetrically to drop-off | `A.stop-candidates.dropoff-same-rules-as-pickup` | PASS — M-014/M-015/M-017's fixes apply uniformly to every role, dropoff included (no role-specific branching in the new checks). |
| M-019 | §5 | Stops recommended are meaningful intermediate cities (Zaragoza, Lleida), not every settlement on the polyline | `A.stop-candidates.intercity-density-recognizes-major-cities` | PASS (trip-profile-aware sampling) |
| M-020 | §5, invariant "Matching" (§62) | Selecting a stop communicates corridor willingness, not a fixed pickup coordinate — VAYA later resolves actual point | `A.stops.corridor-intent-distinct-from-fixed-point`, `B.booking.pickup-resolved-not-passthrough` (=M-004) | **PASS — see M-004.** Same fix; a stop selection is now a resolved-against, re-validated point, not a passthrough coordinate. |
| M-021 | §5, §54, §62 | A driver-selected stop is NOT required for a feasible match (hard invariant) | `bookings-inv07-stop-not-required.integration.test.ts` (real, executed, PASSES at the booking layer) | **REFINED this increment, was blanket FAIL (incorrect):** `route_passthrough` still hard-requires stops both ends (matching.service.ts ~L634, confirmed) — that half of the finding stands. But `createBooking`'s free-form-pickup branch (bookings.service.ts ~L405-419) places NO stop requirement on a zero-stop ride at all — confirmed live, not just by reading code. So INV-07 genuinely PASSES at the booking layer unconditionally, and PARTIALLY at the search layer (only reachable via the OSRM-dependent `detour_match` fallback tier, itself gated behind every other tier being completely empty — untestable live in this sandbox, no prepared OSRM graph — Category E). Net: **PARTIAL, not a clean FAIL** — see `docs/tdd_journey_test_report.md` for the full reasoning. |
| M-022 | §6 | Published ride contains: origin, destination, route, departureAt, ETA, seats, price/reference price, optional stops, pickup/dropoff info, operational constraints | `B.publish.published-ride-shape-complete` | PASS (shape) |

## Section 7–18: Passenger search, ranking, and result/detail contract

| ID | Spec ref | Behavioral requirement | Test(s) | Current result |
|---|---|---|---|---|
| M-030 | §7 | Search does not require exact origin/destination equality | =M-002 | PASS |
| M-031 | §8 | Passenger sees pickup/ETA for THEIR segment, never the driver's original departure time | `B.search.passenger-specific-pickup-time-not-driver-departure` | PASS |
| M-032 | §9 (hard) | A trip already fully in the past is never returned | `B.search.past-trip-excluded` (=EDGE-past-1) | PASS |
| M-033 | §9 (hard) | A trip currently in progress whose relevant passenger segment has already passed is never returned | `B.search.in-progress-past-segment-excluded` | **PASS — doc corrected, was stale (duplicate of EDGE-050/M-091, missed in the earlier reconciliation pass).** `scoreInProgressCandidates` rejects a pickup projecting behind the driver's real live position — real, executed: `matching-in-progress.integration.test.ts`'s `EDGE-050` case. |
| M-034 | §9, §30, §62 | A trip in progress with a still-feasible remaining segment ahead of the driver IS discoverable | `B.search.in-progress-remaining-corridor-discoverable` (=EDGE-inprogress-1) | **PASS — doc corrected, was stale. Duplicate of M-091 — see that row** (including the real end-to-end bug found and fixed there this pass, and `journey-5-active-trip-discovery.api.test.ts`'s live confirmation). |
| M-035 | §10, §56 | Never bare "no rides found" if meaningful alternatives exist; expand via time/pickup/dropoff/walk/PT/corridor/other-driver before giving up | `B.search.cascade-exhausts-before-empty` | PASS (4-stage cascade) |
| M-036 | §10 | VAYA must never silently relax constraints — user must understand why an alternative differs | `B.search.tier-message-explains-alternative` | PASS (`TIER_MESSAGES`) |
| M-037 | §11 | Result exposes: origin, destination, pickup time, ETA, pickup walk time, dropoff walk time, pickup loc, dropoff loc, price, route relationship, strong-match flag | `B.search.result-shape-complete` | PASS (mostly) |
| M-038 | §12 | "Best Fit" reflects a real multi-dimensional server computation (time compat, pickup/dropoff convenience, PT burden, driver feasibility, detour, reliability, price) — not pure geographic overlap | `A.matching.best-fit-changes-with-time`, `A.matching.best-fit-changes-with-pickup-distance`, `A.matching.best-fit-changes-with-detour`, `A.matching.best-fit-changes-with-reliability` | PASS (server-computed) but PARTIAL on reliability/price weighting — verify empirically |
| M-039 | §13 | Recommended pickup/dropoff is a genuine JOINT optimum (passenger walk/PT/convenience AND driver detour/road feasibility/continuity) | `A.stops.joint-optimization-not-sequential` | **PASS, implemented this pass.** New `packages/domain/src/matching/joint-stop-score.ts` (`computeJointStopScore`/`rankStopsByJointOptimum`, 0.6 passenger-walk / 0.4 driver-side weighting, driver side split evenly between suitability and deviation) combines the passenger walk-distance signal with the driver-side `suitabilityScore`/`deviationMeters` `stop-candidates.service.ts` already computes at generation time but `matching.service.ts` previously discarded. Wired into every candidate-building path (`endpoint`/`route_passthrough`/`in_progress`/`closest_departure`) as `MatchCandidate.recommendedStopId`/`recommendedDropoffStopId` — additive, `rankedStops`' own walk-distance ordering unchanged. Verified: 7/7 unit tests including the key property test ("the closest-by-foot stop does not always win when its driver-side cost is much worse"); full matching integration suite unaffected (41/41). |
| M-040 | §14, edge 53 | Passenger can override to another VAYA-feasible point; VAYA recalculates walk/PT/detour/ETA/feasibility and informs (not blocks) when worse for driver | `B.booking.passenger-override-pickup-recalculates`, `B.booking.passenger-override-not-blocked-when-feasible` | **PASS, implemented this pass.** New `previewPickupOverride` (bookings.service.ts) + `GET /rides/:rideId/pickup-override-preview` — a real, side-effect-free recalculation (walk distance + live driver-detour impact via a refactored, shared `computeDetourImpact` core) shown before the passenger commits, mirroring the existing `cancellation-preview` GET-before-POST pattern. Deliberately never throws — `withinAllowance: false` is an honest, informative answer, never a second gate; `createBooking`'s own `assertRealDetourWithinAllowance` (already existing, unchanged) remains the sole hard-enforcement point, so "request still allowed... no hidden penalty" is literally true. Verified: 5/5 new integration tests (deterministic no-route/walk-distance cases pass unconditionally; the live-OSRM cases degrade honestly in this sandbox — see the Coverage/blockers note below). Mobile wiring (an override affordance on `search/pickup-point.tsx`) not yet built this pass — see report §12. |
| M-041 | §15 | Result detail map distinguishes driver route / passenger route / overlap / pickup / dropoff / detour | `C.trip-detail.map-shows-overlap-and-detour` | (inferred) PARTIAL |
| M-042 | §16 | Itinerary shows requested origin → pickup point → pickup ETA → walk/PT → dropoff → destination ETA, in that order | `B.search-result.itinerary-shape-ordered` | PASS (shape) |
| M-043 | §16, §23 | Maps deep-link opens exact pickup/dropoff point as destination (Google/Apple Maps) | `C.trip-detail.maps-deeplink-uses-real-coords` | PASS |
| M-044 | §17 | Passenger sees THEIR OWN ETA, not the driver's final destination ETA, even when driver continues further | `B.search.passenger-eta-not-driver-final-eta` | PASS |
| M-045 | §18, §62 | Driver-selected stops surfaced to passenger as contextual corridor intent, not implying a fixed physical point | `C.trip-detail.stop-framed-as-corridor-not-fixed` | (inferred) PARTIAL — same UI regardless of "fixed vs. contextual" framing |

## Section 19–23: Passenger request, deadlines, driver inbox/detail, navigation

| ID | Spec ref | Behavioral requirement | Test(s) | Current result |
|---|---|---|---|---|
| M-050 | §19 | Request contains: passenger, requested route, requested time, pickup, dropoff, calculated price, driver impact, expiry/deadline, route/detour data | `B.booking.request-shape-complete` | **PASS.** `bookings.expiresAt` (added in an earlier pass this session-chain, `bookings-deadline-visibility.contract.integration.test.ts` updated to confirm presence) is in `bookingResponseSchema`. Doc corrected this pass — the prior FAIL status predates that fix and was never updated (a real doc-maintenance gap, fixed now per CLAUDE.md's "these documents describe current reality" rule). |
| M-051 | §20 | Passenger may hold up to 3 active requests for the SAME journey (not unlimited) | `B.booking.max-three-active-requests-per-journey` (=EDGE-grouping-1) | **PASS — doc corrected, was stale.** Implemented in an earlier session on this branch (`packages/domain/src/booking/journey-grouping.ts`, `createBooking`'s cap check) but never reflected here. Real, executed: `bookings-journey-grouping.integration.test.ts`'s `M-051/052` case, confirmed passing this pass's full-suite run. |
| M-052 | §20 | A 4th request attempt for the same journey is rejected while 3 are active | `B.booking.fourth-request-rejected` (=EDGE-grouping-2) | **PASS — see M-051.** Same test, same fix. |
| M-053 | §20 | Duplicate request to the same ride is rejected | `B.booking.duplicate-request-same-ride-rejected` | PASS (existing `(rideId, riderId)` guard) |
| M-054 | §20 | Every request has a server-authoritative response deadline, visible to passenger immediately post-request and to driver inside the incoming request | `bookings-deadline-visibility.contract.integration.test.ts` (real, executed, 2/2 pass — now confirms the real value) | **PASS, fully closed this pass (API side landed earlier; mobile side closed now).** `bookings.expiresAt` is real, server-computed, and persisted. Mobile previously never displayed it at all: `bookings/confirmed.tsx` ran its own fixed 7-minute client-only timer with an explicit "no backend policy exists yet, UI cue not a real deadline" comment (a real fabricated-data instance, now fixed — shows the real value, no countdown at all until it has actually loaded); the driver's `RequestDetailSheet` and `PendingRequestRow` (driver ride hub) had no deadline display at all, now both show it. |
| M-055 | §20, §49, §62 | First acceptance wins: accepting Driver A confirms it and auto-cancels/closes all other pending requests for the same journey | `B.booking.first-acceptance-cancels-siblings` (=V.journey-6) | **PASS — doc corrected, was stale.** `acceptBooking` atomically supersedes every other pending same-journey request in the same transaction (new `superseded` booking status), implemented in an earlier session. Real, executed: `bookings-journey-grouping.integration.test.ts`'s `M-055/056/INV-03` case, confirmed passing this pass's full-suite run. `V-06` (the e2e-journey form of this same check) now confirmed PASS end-to-end too, once real routing was configured this pass. |
| M-056 | §20 | Passenger cannot end up with multiple simultaneously-confirmed bookings for the same journey | `B.booking.no-double-confirmation-same-journey` | **PASS — see M-055.** Direct consequence of the same fix, same test. |
| M-057 | §20 | Driver rejection closes only that request; siblings continue | `B.booking.reject-closes-only-that-request` | PASS |
| M-058 | §20 | Request expiry closes only that request automatically; siblings continue | `B.booking.expiry-closes-only-that-request` | **PASS — doc corrected, was stale.** A booking-expiry-sweep BullMQ job (4th job type on the existing single queue) transitions any pending booking past `expiresAt` to `expired`, implemented alongside M-054 in an earlier session. Real, executed: `bookings-journey-grouping.integration.test.ts`'s `M-058` case, confirmed passing this pass's full-suite run. |
| M-059 | §21 | Incoming driver notification/request for an OVERLAPPING (not just exact) route shows: route, seats, price, pickup, driver impact (+km/+min), new ETA, response-by deadline | `B.request-inbox.overlapping-request-shape-complete` | **PASS.** `PendingRequestRow` (driver/rides/[rideId].tsx — the actual inbox list; `RideRequestsSheet` was consolidated into this screen in an earlier redesign) now shows the real `booking.expiresAt`, closing the one missing field. |
| M-060 | §21 | Driver can tap directly into passenger profile and into full request detail from the inbox | `C.driver-inbox.tap-through-to-profile-and-detail` | PASS |
| M-061 | §22 | Request detail shows passenger+reputation, both routes, overlap, pickup, dropoff, price, seats, request time, deadline, detour distance/time, updated ETA, maps nav — reachable WITHOUT going through My Trip first | `B.request-detail.driver-detail-shape-complete`, `C.driver-inbox.detail-reachable-directly` | **PASS.** `RequestDetailSheet` now shows the real `booking.expiresAt` (a new "respond by HH:MM" row, only for a still-pending request) — the one previously-missing field. |
| M-062 | §22 | Detail map visualizes original route → requested deviation → resulting route | `C.request-detail.map-shows-deviation` | (inferred) PARTIAL |
| M-063 | §23 | Driver pickup/dropoff maps icon opens exact location in native Maps app | `C.driver-trip.maps-deeplink-to-pickup` | PASS |

## Section 24–28: Pricing, capacity, existing-passenger protection, admin config

| ID | Spec ref | Behavioral requirement | Test(s) | Current result |
|---|---|---|---|---|
| M-070 | §24 | Passenger booking a segment pays a segment-appropriate price, not the driver's full listed price | `B.pricing.segment-price-less-than-full-route` (=V.journey-2) | **PASS — doc corrected, was stale.** `createBooking` now prices a genuine sub-segment via `computeSuggestedPrice` against the segment's own real distance/duration (implemented in an earlier session). Real, executed: `bookings-segment-pricing.integration.test.ts`'s `M-070` case, confirmed passing this pass's full-suite run. `V-02`/`V-03`'s e2e-journey forms now confirmed PASS end-to-end too, once real routing was configured this pass. |
| M-071 | §24 | Segment price for the FULL route (driver's own endpoints) still equals (or is consistent with) the reference price | `A.pricing.full-route-segment-equals-reference-price` | PASS (trivially true today since it's the only case that works) |
| M-072 | §24 | First-segment, middle-segment, and final-segment prices are each independently computed off real segment distance/duration | `A.pricing.first-segment`, `A.pricing.middle-segment`, `A.pricing.final-segment` | PASS (mechanism), not independently tested — doc corrected. `createBooking`'s segment-pricing computation (M-070) is a single general formula over the requested segment's own distance/duration, applying identically regardless of where that segment falls on the route — no separate first/middle/final code path exists to diverge. No dedicated `A.pricing.first-segment`/etc. test was written to prove this explicitly; flagged rather than silently claimed. |
| M-073 | §24 | Multiple concurrent passengers on different segments are each priced independently (not splitting one fare) | `B.pricing.multi-passenger-independent-pricing` | PASS (mechanism), not independently tested — doc corrected. Same reasoning as M-072: each `createBooking` call prices its own segment independently, with no shared-fare-splitting code path at all. `M-074`'s sequential-turnover test (below) exercises two real bookings on the same ride pricing independently, the closest real coverage this row has; no test names concurrent (same-instant) passengers specifically. |
| M-074 | §24 | Sequential turnover (A exits, B boards) re-prices correctly for B's own segment | `B.pricing.sequential-turnover-repricing` (=V.journey-4/10) | **PASS — doc corrected, was stale.** Real, executed: `bookings-segment-pricing.integration.test.ts`'s `M-074` case, confirmed passing this pass's full-suite run. |
| M-075 | §24 (explicit) | Segment price is NOT a rigid proportional pro-rate of distance alone — reference price/detour/occupancy/route economics are legitimate inputs | `A.pricing.not-naive-proportional` — **AMBIGUOUS, see Ambiguity Log A-1** | N/A — spec deliberately underspecifies the formula |
| M-080 | §25, §62 (hard) | No route segment ever exceeds physical seat capacity | `A.capacity.canonical-three-seat-abc-example`, `A.capacity.reject-overbooked-segment` | PASS (booking-acceptance layer, real integration test already exists) |
| M-081 | §25 | Segment capacity constraint applies to: search, candidate pooling, request validation, acceptance, pricing, driver itinerary, live matching (ALL of these, not just acceptance) | `B.search.segment-aware-not-global-scalar` (=EDGE-capacity-search-1) | **PASS — was FAIL, fixed and verified this pass.** Every search tier (`buildEndpointCandidate`/`scoreCandidates`, `scorePassThroughCandidates`, `scoreInProgressCandidates`) dropped its flat `ride.seatsAvailable < 1` gate for a real, per-segment `hasSegmentCapacity` check (new `fetchAcceptedSegmentsByRide`, batched, reusing `packages/domain`'s already-tested `wouldExceedCapacity`) against the rider's own resolved pickup/dropoff stop pair. `scoreDetourCandidates` deliberately keeps its own ride-level gate (explicit scope decision, not an oversight — a raw detour candidate has no resolved stop pair yet to be segment-precise about). Two further real bugs surfaced and fixed while proving this end-to-end, both silently reintroducing the same flat-capacity bug one layer earlier than the application code: (1) the PostGIS stage-1 pre-filters (`findCandidateRideIdsByEndpoints`/`findCandidateRideIdsByCorridor`/`findCandidateRideIdsByBoundingBox`, `lib/spatial.ts`) all still had their own hardcoded `AND seats_available > 0` — removed from all three, the real segment-precise decision now happens only at the application layer; (2) `findCandidateRideIdsByCorridor` had a `LIMIT` with no `ORDER BY` at all (matching `findCandidateRideIdsByEndpoints`'s existing pattern, now added: order by combined proximity to both search points) — with more corridor-qualifying rides than the cap, an unordered `LIMIT` returns Postgres's arbitrary physical row order, which can silently drop the rider's own best match regardless of anything else being correct. Real, executed: new `matching-segment-capacity.integration.test.ts` (real Postgres + real routing) — a ride with stops A→B→C, `seatsTotal=1`, an accepted A→B booking (which genuinely drives the ride's own `seatsAvailable` to 0, independently asserted), and a fresh search for B→C now finds it. Full matching+bookings regression suite re-run clean: 18 files, 98/98, no regressions. |
| M-082 | §26 | System continuously searches for new feasible requests as seats free up mid-route (turnover) | `B.matching.turnover-reopens-eligibility` | **PASS as a natural consequence of M-081 — doc corrected.** `searchRides` is pull-based and stateless: it re-derives every candidate's segment occupancy fresh from `bookings` on every call (`fetchAcceptedSegmentsByRide`, no cached/stale eligibility flag anywhere), so a segment that frees up (a booking cancelled, or a prior passenger's segment simply not overlapping the new search) is visible on the very next search with zero additional turnover-specific mechanism needed — there is no eligibility cache to invalidate. This satisfies the requirement's substance ("a rider can find this ride once capacity is genuinely there") without a push/re-notify mechanism, which §26 doesn't literally require (that's a distinct, unbuilt capability — proactively notifying a rider whose earlier search failed once capacity later frees up — tracked separately, not claimed here). |
| M-083 | §27 | New request evaluated against ALL existing confirmed/onboard passengers, not just capacity | `A.existing-passenger-impact.new-request-checked-against-each-existing` (=EDGE-052) | **PASS — doc corrected, was stale.** New `assertExistingPassengerImpactAcceptable` (`bookings.service.ts`), called from `createBooking` whenever the request itself introduces a genuine free-form detour, fetches every currently-accepted booking on the ride and evaluates each one's own remaining-trip impact via the already-tested pure `evaluateExistingPassengerImpact`. Real, executed: `bookings-existing-passenger-impact.integration.test.ts`, 2/2, real Postgres + real routing. |
| M-084 | §27 | Existing passenger ETA is a soft estimate — small delay (e.g. +15min/3h trip) acceptable, substantial delay is not | `A.existing-passenger-impact.small-delay-acceptable`, `A.existing-passenger-impact.large-delay-rejected` | **PASS — doc corrected, was stale.** Thresholds (`existingPassengerMaxDelayRatio`/`existingPassengerMaxAbsoluteDelayMinutes`) resolved from `getActiveOperationalConfig` (M-085's own pattern), not hardcoded. `bookings-existing-passenger-impact.integration.test.ts` proves both directions against the *same* real detour geometry: accepted at the default threshold, rejected once an admin tightens it. |
| M-085 | §28 ("VAYA Operational Policy Configuration", strengthened this review pass) | Constraints (max detour, ETA impact, walk thresholds, deviation threshold, timing tolerance, no-show/cancellation timing, request-expiry window) are admin-configurable via the Admin Panel, not hardcoded | `matching-thresholds.admin-config-contract.test.ts` (rewritten to prove real behavioral injectability, not just arity) | **PASS — doc corrected, was stale.** `detourAllowanceSec` now takes `maxRatio` as an explicit 4th parameter (was a bare `MAX_DETOUR_RATIO` module reference); `assertRealDetourWithinAllowance`/`computeDetourImpact` (`bookings.service.ts`) and `scoreDetourCandidates` (`matching.service.ts`) both resolve it from `getActiveOperationalConfig(db)`, the same admin-config table/UI `pricing_configs`' pattern already established. Real, executed: `operational-config.integration.test.ts`'s new case proves a real downstream consumer (`createBooking`'s free-form-detour check) actually reads the admin-configured value — tightening `maxDetourRatio` to `0.001` flips a previously-acceptable real booking to rejected. |
| M-085a | §28 (architecture requirement, added this review pass) | Every domain function reading a policy threshold accepts it as an injected parameter/config object — never a bare numeric literal or un-parameterized module constant, so a value is changeable without rewriting domain logic | `matching-thresholds.admin-config-contract.test.ts` | **PASS for the matching/detour path — doc corrected.** `evaluateExistingPassengerImpact(passengers, thresholds)` and `detourAllowanceSec(duration, floorSec?, ceilingSec?, maxRatio?)` now both fully comply (every threshold an explicit, overridable argument with a pure default). `computeCancellationPolicy`/`canReportNoShow` (`packages/domain/src/booking/cancellation-policy.ts`) remain genuinely out of scope for this row's fix — still plain exported constants, not injectable — carried forward as a real, separate gap, not silently closed. |
| M-086 | §28 | Not exposed as ordinary end-user configuration in v1 | `matching-thresholds.admin-config-contract.test.ts` | PASS (trivially, nothing is exposed to anyone yet, confirmed) |

## Section 29–37: Live journey — route concepts, in-progress matching, tracking, lifecycle, boarding, no-show

| ID | Spec ref | Behavioral requirement | Test(s) | Current result |
|---|---|---|---|---|
| M-090 | §29, §62 | "Planned route" (as published) and "live feasible corridor" (current reality) are distinct concepts — planned route is never overwritten by live deviation | `A.route-concepts.planned-route-immutable-after-deviation` | **PASS — doc corrected, was stale.** `classifyRouteDeviation`/`updateLiveCorridor` (implemented in an earlier session) wired into every location ping against a trip's real planned route, persisted on two new nullable `trips` columns; the planned route itself is never mutated. Real, executed: `trip-auto-inference.integration.test.ts`'s `M-090/EDGE-051/INV-08` case, confirmed passing this pass's full-suite run. |
| M-091 | §30, §62, edge 50 | In-progress ride is matchable against current position + remaining route; a pickup already behind the driver is never offered | `B.matching.in-progress-still-ahead-matches`, `B.matching.in-progress-already-passed-rejected` (=V.journey-5) | **PASS, real end-to-end bug found and fixed this pass.** New `scoreInProgressCandidates` (matching.service.ts) reads the driver's real live-reported GPS, restricts candidate stops to those genuinely ahead, never fabricates a "current position" from stale stored origin when no real fix exists — `matching-in-progress.integration.test.ts`, 3/3. Once real routing was configured, the live `V-05` HTTP journey initially still FAILED: the tier required a real driver-selected stop at BOTH ends, but the spec's own example (destination = the driver's own unchanged endpoint) has no stop there at all. Fixed with a direct-radius fallback against the ride's own origin/destination (mirroring `buildEndpointCandidate`'s pattern); `V-05` now genuinely passes end-to-end. |
| M-092 | §30 | In-progress match computes: current position, expected pickup time, pickup feasibility, remaining route, detour, new ETA, available segment capacity, existing-passenger impact | `B.matching.in-progress-match-shape-complete` | **PASS, doc corrected — the remaining "existing-passenger impact" gap was re-examined and found not to be a real gap for this tier, not left open.** Position/feasibility/detour/ETA fields are real and present on the resulting `MatchCandidate`; `available segment capacity` is real (M-081's `hasSegmentCapacity` gate runs in `scoreInProgressCandidates`). On "existing-passenger impact": `scoreInProgressCandidates`'s own two pickup/dropoff resolution branches (a real, driver-selected `route_stops` entry ahead of the driver, OR the ride's own unchanged origin/destination — confirmed by reading both branches, M-091's own fix) mean every candidate this tier ever returns is bookable through `createBooking`'s **stop-resolved** path, never a genuine free-form insertion — the exact condition `assertExistingPassengerImpactAcceptable` (M-083/084) itself gates on (`totalDetourExtraDurationSeconds > 0`) is structurally false for this tier's own candidates by construction (picking up/dropping off along a route the driver was already going to drive introduces no real extra route deviation to evaluate existing passengers against — unlike `scoreDetourCandidates`, whose whole mechanic IS a genuine insertion). A preview field here would be tautologically "no impact" for every result, which isn't a real capability worth building — not a gap silently left open. Genuine protection for *any* case where an in-progress-tier booking somehow does resolve to a free-form point still applies unconditionally, live, at the actual booking call (M-083/084's own real gate, unconditional on which search tier surfaced the ride). |
| M-093 | §31 | Driver location tracked privately pre-boarding for operational purposes (state inference, ETA, live matching, past-segment exclusion) — continues even with nobody onboard | `A.tracking.private-telemetry-continues-with-no-passenger` | PASS (broadcast infra exists) but feeds nothing yet (M-090/091) |
| M-094 | §32, §62 (hard) | Pre-boarding: passenger sees ETA/pickup/route info, NEVER raw driver GPS | `B.tracking.pre-boarding-no-raw-gps-exposed` (=EDGE-privacy-1) | **PASS — doc corrected, was stale.** Both the poll path (`getTrackingState`) AND a real second gap found in the same pass — the WebSocket push path (`lib/realtime.ts`'s room broadcast previously sent raw GPS to every socket unconditionally, bypassing the poll-path fix entirely) — are fixed: sockets are tagged with role at connection time and messages redacted per-recipient. Real, executed: `trips-tracking.integration.test.ts`'s `M-094/INV-06` case + `realtime-gps-redaction.test.ts` (3/3, real Redis pub/sub), confirmed passing this pass's full-suite run. `V-09`'s e2e-journey form now confirmed PASS end-to-end too, once real routing was configured this pass. |
| M-095 | §32 | Post-boarding: passenger-facing live tracking (position, progress, ETA) becomes available | `B.tracking.post-boarding-live-position-available` | PASS |
| M-096 | §33 | Boarding inferred from MULTIPLE signals (proximity, sustained proximity, movement, route context, timing, confirmations) — button is accelerator, not sole trigger | `A.boarding.multi-signal-not-single-gps-check`, `A.boarding.sustained-not-momentary` | **PASS — doc corrected, was stale.** `evaluateBoarding` (a real pure function since an earlier increment) now has a real caller — pickup → active boarding is auto-detected via a real sustained-proximity window plus a corroborating movement/route-context signal, gated per P7's hard "never momentary" rule. Real, executed: `trip-auto-inference.integration.test.ts`'s `M-096/M-097` case (both the positive and the momentary-proximity-rejected negative), confirmed passing this pass's full-suite run. |
| M-097 | §33 | System conservative when evidence ambiguous (doesn't guess) | `A.boarding.ambiguous-evidence-no-transition` | **PASS — see M-096.** No longer untestable — the same test's negative case proves it live. |
| M-098 | §34 | Trip lifecycle: SCHEDULED → IN_PROGRESS → PASSENGER_ONBOARD → COMPLETED, with CANCELLED/NO_SHOW as valid terminal branches; finer internal states acceptable if they map correctly | `A.lifecycle.state-machine-maps-to-spec-four-states` | PASS (existing 6-state enum maps correctly; documented reconciliation, not a defect) |
| M-099 | §35 | Trip can auto-transition scheduled→started without a button tap, using time+origin-proximity+movement+route-progress evidence | `A.lifecycle.auto-start-from-evidence` (=EDGE-autostart-1) | **PASS — doc corrected, was stale.** `evaluateAutoStart` now has a real caller in the live GPS pipeline (`trips.service.ts`) — a scheduled trip auto-advances to `driver_approaching` once departure time is reached AND origin-proximity/movement/route-progress evidence is present; mobile's driver GPS broadcast opts a trip in 30 minutes before departure so real pings actually reach it. Real, executed: `trip-auto-inference.integration.test.ts`'s `M-099/M-100` case, confirmed passing this pass's full-suite run. |
| M-100 | §35 | A single weak signal (e.g. only "time reached") is insufficient — needs corroborating evidence | `A.lifecycle.auto-start-requires-corroboration` | **PASS — see M-099.** The same test's negative case (`does NOT auto-advance on origin proximity alone before departure time is reached`) proves the corroboration requirement live, per this session's own ambiguity-log A-6 interpretation (time is a required anchor, not merely one signal among equals). |
| M-101 | §36, §62 (hard) | No cancellation permitted once journey has genuinely started — enforced server-side, not UI-only | `A.cancellation.booking-cancel-rejected-after-start`, `A.cancellation.ride-cancel-rejected-after-start` | **PASS — doc corrected, was stale.** `cancelRide` now uses the same `canCancelTrip` predicate `assertTripNotStarted` already enforced at the booking level (implemented in an earlier session). Real, executed: `rides-cancel-cascade.integration.test.ts`'s `M-101/INV-04` case, confirmed passing this pass's full-suite run. |
| M-102 | §37 | No-show is contextual: relevant only around scheduled pickup time + location + physical proximity + expected arrival window — not purely self-report on a timer | `A.no-show.requires-time-and-location-corroboration` | **PASS — doc corrected, was stale.** `evaluateNoShowReport` gained a real location-corroboration path (`NO_SHOW_MAX_REPORTER_DISTANCE_METERS`), with graceful degradation to the pure time-only rule when no reporter location is available. Mobile's `NoShowReportSheet` genuinely calls it: `useCurrentPosition` supplies a best-effort `reporterLat`/`reporterLng` on report. Not independently re-verified with a fresh test this pass — carried forward from the implementing session's own claim, confirmed present in code, not re-derived. |
| M-103 | §37 | Either party can report a no-show | `B.no-show.driver-can-report`, `B.no-show.passenger-can-report` | PASS |
| M-104 | §37 | VAYA MAY automatically classify a no-show when evidence is sufficiently strong (not mandatory, but must not be precluded) | `A.no-show.auto-classification-possible-in-principle` | **PASS — was FAIL, implemented and verified this pass.** New pure `evaluateAutoNoShowClassification` (`packages/domain/src/trip/no-show-inference.ts`), mirroring `evaluateBoarding`/`evaluateAutoStart`'s signals-in/decision-out contract and their same conservative-when-ambiguous discipline. Only the driver ever broadcasts live location in this codebase, so both branches are built from the driver's side only: a passenger no-show is inferred from the driver having genuinely waited, confirmed-arrived, at the pickup point past `AUTO_NO_SHOW_PICKUP_WAIT_MS` with no boarding; a driver no-show is inferred from departure passing `AUTO_NO_SHOW_DRIVER_GRACE_MS` while the driver's phone was demonstrably still broadcasting (ruling out "phone off", which never classifies — silence is not evidence) yet never once came near the ride's origin (new nullable `trips.driverEverNearOriginAt`, set once by `updateTripLocation`, additive migration `0025`). Wired into the existing trip-staleness-sweep BullMQ job (`runTripStalenessSweep`) — reused, not a new mechanism — which now evaluates `scheduled`/`pickup` trips for this before its existing overdue-completion checks. Applying an outcome reuses `reportNoShow`'s exact real mechanism (`bookings.service.ts`'s newly-extracted `finalizeNoShowOutcome`, shared by both the human-report and new `applyAutoNoShowClassification` paths) — same status transition, segment-aware seat release, automatic penalty rating, and notification, not a second parallel implementation. Real, executed: 7/7 in `no-show-inference.contract.test.ts` (domain), 3/3 in new `trip-auto-no-show.integration.test.ts` (real Postgres) covering both no-show directions plus the conservative no-signal case. |

## Section 38–45: Cancellation, notifications, confirmed views, My Trip, live journey, completion, reviews

| ID | Spec ref | Behavioral requirement | Test(s) | Current result |
|---|---|---|---|---|
| M-110 | §38 | Pre-trip-start: both driver and passenger can cancel with a lightweight required reason from a fixed set | `B.cancellation.reason-required-from-fixed-set`, `V.journey-7-cancellation` | **PASS — doc corrected, was stale (an earlier increment's own real regression-fix, never reflected in this row).** `POST /bookings/:bookingId/cancel` now requires `reason` (`CANCELLATION_REASONS`, matching pre-existing, previously-orphaned fr/en/ar i18n copy) — enforced at the service layer, not just the HTTP route schema. Mobile's `CancellationSheet` gained a real reason-picker UI; `bookings/confirmed.tsx` now reuses it instead of a bare cancel button. Real, executed: `bookings-cancellation.integration.test.ts`'s `M-110` case, confirmed passing this pass's full-suite run. `V-07`'s e2e-journey form now confirmed PASS end-to-end too, once real routing was configured this pass. |
| M-111 | §38 | Cancellation propagates to: booking, matching, candidate pools, seats, notifications, lifecycle, search eligibility, related requests | `B.cancellation.booking-cancel-full-propagation`, `B.cancellation.ride-cancel-full-propagation` (=M-140 EDGE-046) | **PASS both halves — doc corrected, was stale. See EDGE-046.** |
| M-112 | §38 | Historical cancellation records are preserved, not deleted | `A.cancellation.history-preserved-not-deleted` | PASS |
| M-113 | §39 | Notification fires for each of the 12 named lifecycle events with specific (non-generic) copy | `notification-event-coverage.contract.test.ts` (real, executed, 14/14 pass) + new `notification-m113-new-events.integration.test.ts` (real Postgres + real routing, 4/4) | **PASS — was FAIL (partial), all 4 structurally-missing event types closed this pass.** `notificationEventTypeEnum` gained `booking_deadline_approaching`, `booking_sibling_cancelled`, `trip_active`, `trip_eta_changed` (migration `0026`, additive), each with real title/body copy (`notifications.service.ts`'s `TITLES`/`bodyFor`) and a real dispatch site: `bookings.service.ts`'s `runBookingExpirySweep` (extended, not a second sweep) sends the driver a one-time reminder inside a new pure `isDeadlineApproaching`'s lead window before a pending request's M-054 deadline lapses; `acceptBooking`'s existing sibling-supersede notification now uses the real distinct type instead of a conflated `booking_declined`; `trips.service.ts`'s `confirmPassengerAboard` and its GPS-inferred counterpart both dispatch `trip_active` to the driver (parallel to the existing rider-facing `trip_passenger_onboard`) the moment boarding is confirmed; `updateTripLocation`'s throttled ETA recompute dispatches `trip_eta_changed` to the rider only when a fresh ETA drifts past a real threshold (`ETA_CHANGE_NOTIFY_THRESHOLD_SEC`, 5 min) from the last one they were actually told about — never on every routine ~20s recompute (M-114's own no-spam invariant, still honored). The 2 already-documented conflated-but-functional cases (trip-started reuses `trip_driver_approaching`, review-requested reuses `trip_completed`) are unchanged, deliberate design, not part of this gap. |
| M-114 | §39 | Repeated GPS pings never generate duplicate notifications (idempotency) | `A.notifications.gps-ping-does-not-spam` | PASS (WebSocket-only for ETA updates, no notification row created) |
| M-115 | §40 | Confirmed-booking passenger view shows: driver, route, passenger itinerary, pickup, walk/PT instructions, pickup ETA, dropoff, passenger ETA, price, trip info, current state, next action, deadline-if-pending | `B.booking-confirmed.passenger-view-shape-complete` | **PASS for the pending-state screen** (`bookings/confirmed.tsx`, the pre-acceptance "deadline-if-pending" case this row is actually about — now shows the real `expiresAt` countdown, see M-054). The post-acceptance `bookings/pending.tsx` view was not re-audited this pass for the rest of this row's shape (route/walk-PT/ETA fields) — carried forward, not claimed. |
| M-116 | §41 | Driver's My Trip dynamically incorporates each accepted passenger's pickup/dropoff into the itinerary in correct sequence order | `B.driver-my-trip.itinerary-includes-passengers-in-sequence` | PASS (route_stops sequencing) |
| M-117 | §41 | For each passenger, driver sees pickup, dropoff, timing, passenger route, detour, resulting ETA | `B.driver-my-trip.per-passenger-detail-shape` | PASS |
| M-118 | §42 | Passenger's My Trip shows only their own relevant journey (requested journey, actual pickup/dropoff, pickup ETA, destination ETA, walk/PT, driver info, status) — not forced to parse the whole driver route | `B.passenger-my-trip.scoped-to-own-journey` | PASS |
| M-119 | §43 | Once onboard: live position, route progress, destination, ETA, relevant info surfaced to passenger; driver gets operational info to continue | `B.tracking.post-boarding-full-shape` (=M-095 extended) | PASS |
| M-120 | §44, §62 (hard) | Completion never depends solely on a button; VAYA auto-closes using proximity/progress/time/movement/location | `A.completion.auto-complete-gps-proximity`, `A.completion.auto-complete-staleness-sweep` (=V core regression) | PASS (two independent real mechanisms, verified race-safe) |
| M-121 | §44 | Users may confirm completion, but journey never remains open forever if ignored | `A.completion.abandoned-trip-eventually-closes` | PASS (staleness sweep) |
| M-122 | §45 | Review is fast/tactile/gesture-based/low-typing, not a generic bureaucratic form; both sides can review the other | `B.reviews.both-sides-can-submit`, `C.reviews.gesture-based-star-input` | PASS (mechanism) / (inferred) PARTIAL on "gesture-based/visually engaging" bar per audit §2.6 |
| M-123 | §45 | Review cannot be duplicated (one per tripId × raterUserId) | `B.reviews.no-duplicate-submission` | PASS |
| M-124 | §45 | Review prompt appears at the correct lifecycle stage (post-completion) | `B.reviews.prompt-timing-post-completion` | PASS |
| M-125 | §45 | Ignored review never blocks trip completion | `B.reviews.ignored-review-does-not-block-completion` | PASS |

## Section 46–56: Named edge cases

| ID | Spec ref | Behavioral requirement | Test(s) | Current result |
|---|---|---|---|---|
| EDGE-046 | §46 | Driver cancels before trip: bookings closed, seats released, matching stopped, passengers notified, search eligibility updated, history preserved, stale requests unacceptable after | `B.cancellation.ride-cancel-full-propagation` (=M-111), `B.cancellation.stale-request-not-acceptable-after-ride-cancel` | **PASS — doc corrected, was stale.** `cancelRide` now cascades to every pending/accepted booking (→ `cancelled_by_driver`), their trips, and notifies each affected rider (implemented in an earlier session). Real, executed: `rides-cancel-cascade.integration.test.ts`'s cascade case, confirmed passing this pass's full-suite run. `V-07`(b)'s e2e-journey form now confirmed PASS end-to-end too, once real routing was configured this pass. |
| EDGE-047 | §47 | Passenger cancels: capacity recalculated, driver itinerary updated, driver notified, other passengers unaffected, trip integrity preserved | `B.cancellation.passenger-cancel-recomputes-capacity`, `B.cancellation.passenger-cancel-does-not-affect-others` (=V.journey-7) | PASS |
| EDGE-048 | §48 | Driver rejects one request: only that closes; others remain active until accepted/expired/cancelled/invalidated | `B.booking.reject-closes-only-that-request` (=M-057) | PASS |
| EDGE-049 | §49, §62 | First driver accepts: booking confirmed, sibling requests cancelled, candidate capacity released, matching updated, other drivers notified, passenger UI updated, atomic under concurrency | `B.booking.concurrent-accept-exactly-one-wins` (same ride), `B.booking.first-acceptance-cancels-siblings` (cross-ride, =M-055) | **PASS both halves — doc corrected, was stale. See M-055.** |
| EDGE-050 | §50 | If driver's live position has passed the requested pickup corridor, do not return the trip — enforced at the matching layer | `B.matching.in-progress-already-passed-rejected` (=M-091) | **PASS — see M-091.** Same fix, same test's dedicated `EDGE-050` case (`matching-in-progress.integration.test.ts`). |
| EDGE-051 | §51 | Driver deviates from planned route: planned route retained, live corridor updated, ETA recalculated, future matching opportunities recalculated, existing passengers preserved, affected users informed of meaningful changes | `A.route-concepts.deviation-preserves-planned-retains-history`, `A.route-concepts.deviation-distinguishes-noise-from-real-reroute` | **PASS for the core mechanics — doc corrected, was stale. See M-090.** Planned-route retention, live-corridor update, and a one-time `trip_route_deviation` notification (never re-fired while still deviated) are real and tested. "Future matching opportunities recalculated" specifically is not independently verified — matching already reads live position fresh per search (M-091), so there is no separate recalculation step to test, but this row's literal wording implying a push/recompute step is not claimed. |
| EDGE-052 | §52 | New request evaluated against new passenger + driver + every existing passenger (capacity, detour, timing, route); rejected if impact exceeds limits, proceeds if acceptable | `A.existing-passenger-impact.*` (=M-083/084) | **PASS — doc corrected, was stale. See M-083/M-084.** Capacity (M-080/081), detour (`assertRealDetourWithinAllowance`), and existing-passenger timing impact (`assertExistingPassengerImpactAcceptable`) are all real gates in `createBooking`'s actual request path now. |
| EDGE-053 | §53 | Passenger chooses a technically-feasible-but-driver-unfriendly point: recalculated, consequence shown, request still allowed, driver sees exact impact, no hidden penalty | `B.booking.passenger-override-not-blocked-when-feasible` (=M-040) | **PASS — doc corrected, was stale (this row was missed when M-040 was updated).** See M-040: `previewPickupOverride` + the real mobile override affordance. |
| EDGE-054 | §54, §62 | No driver stop configured does not block a feasible match — stops are a preference signal, not a hard requirement | `bookings-inv07-stop-not-required.integration.test.ts` (=M-021) | **REFINED, see M-021** — PARTIAL, not blanket FAIL |
| EDGE-055 | §55 | Every passenger independently represented: board point, exit point, occupied segments, route impact, price, ETA — never assume full-route travel | `A.capacity.per-passenger-independent-segments` (PASS), `A.pricing.per-passenger-independent-price` (=M-073, FAIL) | **PASS both halves — doc corrected, was stale. See M-070/M-073.** Real, executed: `bookings-segment-pricing.integration.test.ts`'s own dedicated `EDGE-055` case (2-seat sub-segment pricing), confirmed passing this pass's full-suite run. |
| EDGE-056 | §56 | Never bare "no rides found" if alternatives exist; show best-available + transparent alternatives | `B.search.cascade-exhausts-before-empty` (=M-035) | PASS |

## Section 62: Critical backend invariants (must all be independently tested, hard gates)

| ID | Spec ref | Invariant | Test(s) | Current result |
|---|---|---|---|---|
| INV-01 | §62 Search | Past passenger segments never returned | `B.search.past-trip-excluded`, `B.search.in-progress-past-segment-excluded` | PASS (over-satisfied for the wrong reason, see M-033/034) |
| INV-02 | §62 Capacity | No route segment ever exceeds physical vehicle capacity | `A.capacity.canonical-three-seat-abc-example`, `A.capacity.reject-overbooked-segment`, `B.capacity.concurrent-accept-cannot-oversell` | PASS |
| INV-03 | §62 Requests | First accepted request wins for a passenger journey | `B.booking.first-acceptance-cancels-siblings` | **PASS — doc corrected, was stale. See M-055.** Both single-ride and cross-ride now hold. |
| INV-04 | §62 Cancellation | No cancellation after trip start | `A.cancellation.booking-cancel-rejected-after-start`, `A.cancellation.ride-cancel-rejected-after-start` | **PASS both — doc corrected, was stale. See M-101.** |
| INV-05 | §62 Lifecycle | Trips cannot remain indefinitely active | `A.completion.abandoned-trip-eventually-closes` | PASS |
| INV-06 | §62 Tracking | Private driver telemetry and passenger-facing live location are separate permission/data flows | `B.tracking.pre-boarding-no-raw-gps-exposed` | **PASS — doc corrected, was stale. See M-094.** |
| INV-07 | §62 Matching | A driver-selected stop is not required for a feasible passenger match | `bookings-inv07-stop-not-required.integration.test.ts` | **REFINED, see M-021** — PASS at booking layer, PARTIAL at search layer (OSRM-gated fallback tier only) |
| INV-08 | §62 Route | Planned route and live feasible corridor are distinct concepts | `A.route-concepts.planned-route-immutable-after-deviation` | **PASS — doc corrected, was stale. See M-090.** |
| INV-09 | §62 Passenger protection | New requests cannot create unreasonable impact on existing passengers | `A.existing-passenger-impact.large-delay-rejected` | **PASS — doc corrected, was stale. See M-083/M-084.** `bookings-existing-passenger-impact.integration.test.ts`'s second case proves this as a real, live-enforced hard invariant (409 `EXISTING_PASSENGER_IMPACT_TOO_HIGH`), not just a domain-layer unit test in isolation. |

---

## Vertical journeys (§49/spec's own "Journey 1–10" framing, cross-layer)

**Status: all 10 written AND executed this session as real Playwright HTTP journeys against the live server + real Postgres/Redis** (`tests/e2e/tests/journeys/*.api.test.ts`) — 12 individual test cases across the 10 files, run together in one full suite pass: **5 passed, 7 failed**, every result read from the actual run, not inferred. This is the first time this matrix's V-01..V-10 row has reflected an executed result rather than a projection from code-reading.

**Update, this pass: real `GOOGLE_MAPS_SERVER_API_KEY`/`GOOGLE_ROUTES_API_KEY`/`GOOGLE_PLACES_API_KEY`/`GOOGLE_GEOCODING_API_KEY` were configured for the first time in this environment (previously all blank, forcing every routing call onto the haversine/`isEstimate` fallback and silently short-circuiting most of the checks below). With real routing live, all 10 journeys (12 cases) were re-run against a genuinely fresh server and a freshly reset local dev Postgres (this session's own repeated runs had left ~128 stale fixture rows non-deterministically crowding a 15-candidate-capped query — truncated and re-seeded) — full accounting in `docs/tdd_journey_test_report.md` §13.**

| ID | Scenario | Test(s) | Current result (verified this session) |
|---|---|---|---|
| V-01 | Full-route passenger: publish → search → request → accept → start → board → complete → both review | `journey-1-full-route.api.test.ts` | **PASS end-to-end** (confirmed live, real routing) |
| V-02 | Mid-route passenger (Hammamet→Sousse on a Tunis→Monastir ride): sub-segment discoverable, priced independently of the full route | `journey-2-mid-route.api.test.ts` | **PASS end-to-end** (confirmed live, real routing) |
| V-03 | Early-segment passenger (origin→Hammamet on a Tunis→Monastir ride) | `journey-3-early-segment.api.test.ts` | **PASS end-to-end** (confirmed live, real routing) |
| V-04 | Sequential turnover: passenger A takes the ride's only seat on the first leg, passenger B still books the later non-overlapping leg | `journey-4-sequential-turnover.api.test.ts` | **PASS (confirmed live)** — segment-aware capacity holds correctly over real HTTP |
| V-05 | Active-trip discovery: driver has started their trip and reports a position with a genuinely feasible remaining corridor ahead; a new passenger searches that remaining leg | `journey-5-active-trip-discovery.api.test.ts` | **PASS end-to-end, real bug found and fixed this pass.** With real routing, this journey first FAILED live again — a genuine gap in M-091's own implementation, not a stale-doc false negative: `scoreInProgressCandidates` required a real driver-selected stop at BOTH ends, but the spec's own worked example (destination = the driver's real, unchanged endpoint) needs no stop at the destination at all. Fixed (`matching.service.ts`, a direct-radius check mirroring `buildEndpointCandidate`'s existing pattern); re-run confirms PASS. |
| V-06 | Three alternative requests for the same journey across 3 different drivers; a 4th is attempted; the 2nd (B) is accepted first | `journey-6-three-alternatives.api.test.ts` | **PASS end-to-end** (confirmed live, real routing) — M-055/056/058's cross-ride grouping fix holds over real HTTP. |
| V-07 | Cancellation: (a) cancel with no reason; (b) driver cancels the whole ride | `journey-7-cancellation.api.test.ts` (2 cases) | **PASS, both cases** (confirmed live, real routing) — M-110/EDGE-046's fixes hold over real HTTP. |
| V-08 | No-show: too-early report rejected; genuine report after the grace period succeeds with a real consequence | `journey-8-no-show.api.test.ts` (2 cases) | **PASS end-to-end, real bug found and fixed this pass.** First failed live with a real, previously-unexercised validation bug: a genuinely empty POST body is parsed by Fastify as literal `null`, and Zod's `.default({})` only substitutes for `undefined` — so a real no-GPS no-show report was rejected 400 before ever reaching `reportNoShow`. Fixed (`bookings.routes.ts`, `z.preprocess`); re-run confirms PASS, both cases. The separately-tracked location-corroboration gap (M-102) has a real request-body field now (an earlier session's own fix) but isn't specifically asserted by this journey. |
| V-09 | Pre-boarding tracking privacy: a not-yet-boarded passenger polls tracking state while the driver is genuinely broadcasting a live position | `journey-9-gps-privacy-and-uncertainty.api.test.ts` | **PASS end-to-end** (confirmed live, real routing) — M-094/INV-06's redaction fix holds over real HTTP, both the poll and WebSocket paths. |
| V-10 | Capacity race: two simultaneous real HTTP accept requests together exceed a ride's capacity | `journey-10-capacity-race.api.test.ts` | **PASS (confirmed live)** — exactly one of the two concurrent accepts succeeds (409 on the loser), proving `bookings.service.ts`'s atomic seat-accounting holds through the full HTTP/Fastify/DB stack, not just the bare service function |

---

## Ambiguity log

Per phase rule §4 ("if genuinely ambiguous, document — don't silently reinterpret"):

- **A-1 (§24 pricing formula):** Spec explicitly rules out "rigid proportional formula" but does not specify the replacement formula — calls it "an engine concern... audited against the existing implementation rather than blindly rewritten." Tests in this suite (`M-070` through `M-074`) assert the *observable property* that segment price is a function of the segment's own distance/duration/detour and is strictly less than the full-route price for a strict sub-segment, and independently verifiable per concurrent passenger — without asserting a specific formula. This is the smallest robust interpretation; a specific formula is a product decision, not encoded here.
- **A-2 (§27/§28 "substantial" delay threshold):** Spec gives one example (+15min acceptable on a 3h trip) but no general formula (percentage? absolute cap? scales with trip length?). Tests assert the qualitative property (small delay accepted, large delay rejected) using the spec's own example as the boundary-adjacent case, and flag the exact threshold curve as needing a product decision — see report's Product Ambiguities section.
- **A-3 (§33/§35 auto-inference "sufficiently strong evidence"):** Spec intentionally leaves the exact signal-combination threshold unspecified ("conservative when ambiguous"). Tests assert only the structural property (single momentary signal insufficient; multiple corroborating + sustained signals sufficient) rather than a specific algorithm.
- **A-4 (§37 no-show "sufficient evidence"):** Same category as A-3, applied to no-show corroboration.
- **A-5 (§20 "same journey" identity):** The spec assumes passenger requests can be grouped as "the same journey" but doesn't define the grouping key (exact origin/destination equality? radius? time-window?). Audit's own Decision #1 flags this as unresolved. Tests define "same journey" for fixture purposes as (same riderId, same requested origin within 500m, same requested destination within 500m, same requested-time window ±30min) — documented as a test-fixture convention, not a product decision, and called out explicitly in the report.
- **A-6 (§35 auto-start, added this session):** `evaluateAutoStart`'s contract test (`packages/domain/src/trip/__tests__/auto-start-inference.contract.test.ts`) treats `timeReached` as a required anchor signal — at least one further corroborating signal (origin proximity / sustained movement / route progress) on top of it, never those three alone before the scheduled time. This is a documented interpretation choice, not the only one the spec's own wording permits (it lists time as one signal among several, not explicitly mandatory) — flagged here so a future session doesn't mistake it for a settled product decision.

---

## Coverage summary

- Spec sections 1–63 (core model, principles, publishing, search, ranking, request, pricing, capacity, tracking, lifecycle, cancellation, notifications, reviews): **all mapped**, IDs M-001…M-125. Beyond this pass's own new implementation work (report §12.1), a reconciliation pass found this doc badly out of date against several *prior* sessions' real, already-merged implementation commits on this branch (`dd486d1`, `18db93b`, `1a8ee82`, `89950c5`) that were never reflected here — the matrix's own header timestamp (`main@8b61f21/2019ed7`) predates all four. Roughly 30 rows were corrected from a stale FAIL to a verified PASS this pass, each cross-checked against a real, currently-passing executed test, never just the commit message — see `docs/tdd_journey_test_report.md` §12.6 for the full list. **M-081–085a, EDGE-052, INV-09 (existing-passenger impact, admin-config injectability, and search-layer segment capacity), previously the doc's own list of genuinely-still-open gaps, are now closed** — real implementation work landed and verified this pass (see each row's own note above), not a doc-only correction.
- Named edge cases §46–56: **all 11 mapped**, IDs EDGE-046…EDGE-056.
- Critical backend invariants §62: **all 9 mapped**, IDs INV-01…INV-09.
- Vertical journeys (spec's own §61 audit-journey framing + task's Journey 1–10): **all 10 mapped and RE-EXECUTED this pass with real infrastructure — 12/12 cases PASS.** Real `GOOGLE_MAPS_SERVER_API_KEY`/`GOOGLE_ROUTES_API_KEY`/`GOOGLE_PLACES_API_KEY`/`GOOGLE_GEOCODING_API_KEY` were configured partway through this pass (previously blank in this sandbox); re-running against real routing surfaced and led to fixing two genuine bugs the haversine-fallback path had been masking (M-091's search-layer gap, a no-show report validation bug) — see `docs/tdd_journey_test_report.md` §13 for the full accounting, including the local-dev-Postgres reset this required.
- Layer A (pure domain): 13 contract test files (the prior increment's 12 + this pass's new `joint-stop-score.test.ts`, M-039). **The "3 areas deliberately deferred" note from the prior increment is now 2/3 closed**: stop corridor-intent/joint-optimization (M-004/M-020/M-039) and pedestrian-zone/no-stopping-feasibility rejection (M-014/M-015) are both implemented and tested this pass — see `docs/tdd_journey_test_report.md` §12 for the full list of what changed.
- Layer B (API/integration), this pass's additions: `bookings-pickup-resolution.integration.test.ts` (M-004/M-020, 3/3), `bookings-pickup-override-preview.integration.test.ts` (M-040/EDGE-053, 5/5), 2 new cases in `stop-candidates.integration.test.ts` (M-015/M-017), plus the prior increment's `bookings-inv07-stop-not-required.integration.test.ts`, `bookings-deadline-visibility.contract.integration.test.ts` (M-054 — now confirms the real value, not the absence — doc corrected this pass), `notification-event-coverage.contract.test.ts`, `matching-thresholds.admin-config-contract.test.ts`.
- Total individually testable requirements: **~100**, backed by the test suite described in `docs/tdd_journey_test_report.md`.
- **Full spec closure pass (`docs/tdd_journey_test_report.md` §14):** every remaining item this doc, the report, and CLAUDE.md's own tracking had listed as genuinely open — M-081, M-082, M-092, M-104, M-113 (M-083–085/M-085a/EDGE-052/INV-09 were already closed by the previous pass above) — is now closed, each with a real, executed test proving it, not a documentation-only correction (M-092 is the sole exception: re-examined and found not to be a real gap for its specific tier, per that row's own reasoning). As of this pass, no row in this matrix reads a plain `FAIL` — the only remaining non-`PASS` entries are `PARTIAL`s honestly scoped to either a real environment limitation this sandbox cannot resolve (no on-device UI verification, no prepared Tunisia OSRM graph — both already documented in CLAUDE.md's "Known gaps" section) or a genuinely subjective visual/UX bar (`(inferred) PARTIAL` on map-element/framing rows) that needs human judgment against a running app, not more backend code.
