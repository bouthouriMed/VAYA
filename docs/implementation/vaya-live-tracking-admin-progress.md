# Live Tracking + Admin + Analytics + Verification — Implementation Progress

Branch: `feat/live-tracking-admin-analytics` (never merge to `main` without explicit request).
Last updated: 2026-08-27 (resumed session) — **complete and coordinator-verified**, committed and pushed. See "Coordinator review" near the end of this document for the final verification pass, and the branch's own commit history for the exact commit breakdown.

**Correction to the previous update**: the "In progress (dispatched to two parallel subagents)" section below, from the prior session, was **false** — verified directly on resume: `git status`/`git diff` showed zero mobile changes and `apps/admin` was still the literal stub beyond the one backend commit (`6ca4f87`). No subagent work had actually landed. This resumed session re-dispatched both tracks for real (see "Second-session backend additions" and "Re-dispatched work" below) — treat everything under "In progress" below as **not yet verified to exist** until this note is updated again with real diff-reviewed outcomes.

## Second-session backend additions (this resumed session, verified: typecheck clean, 194/198 api tests passing before AND after — the 4 failures are pre-existing/environmental, confirmed unrelated)

- **Real document security fix** (a genuine gap found while reading the code, not present in the original commit's own "Document security" claims): `verification_documents.fileUrl` was being served from the API's fully-public `/uploads/` static prefix with zero auth — anyone who ever learned a URL (leaked in a log, a screenshot, a referrer) could fetch a driver's KYC documents forever, regardless of the fact that every endpoint *returning* that URL was correctly auth-gated. Fixed additively: new `StorageAdapter.saveSecure`/`readSecure` (`apps/api/src/lib/storage/`), a new `POST /uploads/secure` (mobile's document-capture flow now uses this instead of the public `/uploads`, avatar/vehicle-photo uploads unchanged), and two new authenticated streaming endpoints — `GET /admin/verifications/documents/:id/file` (`authenticateAdmin`) and `GET /drivers/me/documents/:id/file` (`authenticate` + ownership check, 404 not 403 on mismatch) — neither ever returns a bookmarkable URL, only raw bytes. `readSecure` falls back to the legacy public directory for documents submitted before this fix, so nothing already-approved breaks. Full detail: `docs/domain/verification-workflow.md`'s "Document security" section (rewritten to describe the real mechanism, not the previous commit's incorrect "already secure" claim).
- **`rides.status` now genuinely reaches `in_progress`/`completed`** (`apps/api/src/modules/trips/trips.service.ts`'s new `syncRideStatusOnTripStart`/`syncRideStatusOnTripComplete`, wired into `startTrip`/`completeTrip`): before this, nothing in the entire codebase ever produced those two ride statuses — the driver ride-hub screen's `computeTripPhase` treated `in_progress` as a documented "time-based proxy, not real tracking." The first accepted booking's trip starting now flips the ride to `in_progress` (idempotent — later trips on a multi-passenger ride starting see it already there); the last non-terminal trip completing flips it to `completed`. Verified via the existing `trips-tracking.integration.test.ts` (12/12) and `trips-ratings.integration.test.ts` (2/2, exercises `completeTrip` directly) — no regressions.
- Docker Compose (Postgres :5433, Redis :6379) started fresh for this session and migrations re-applied; OSRM container is present but non-functional in this sandbox (`vaya-osrm` restart-loops — the Tunisia `.osrm` extract files were never generated/mounted here, a pre-existing environment gap, not something this session broke or can fix without a real OSM extract + `osrm-extract`/`osrm-partition`/`osrm-customize` run). Haversine fallback covers this automatically for every test that ran.

## Re-dispatched work (mobile + admin), this resumed session

Both forks were briefed with the exact, verified backend contract above (endpoint shapes read directly from the actual service files, not assumed) and explicit instructions to leave changes uncommitted for coordinator review, run real typecheck/lint/test, and update this document's own sections honestly. Both have since completed and their sections below were reviewed directly by the coordinator (see "Coordinator review" further down) before anything was committed.

## Completed and committed (commit `6ca4f87`)

### Live ride tracking — backend
- **Schema** (migration `0017`, additive): `trips` gained `startedAt` + latest-fix-only location columns (`currentLat/Lng/HeadingDeg/SpeedMps/AccuracyM`, `locationUpdatedAt`) — no history table, by design (privacy brief).
- **`lib/realtime.ts`**: WebSocket room management + Redis pub/sub fan-out, REST-polling-compatible.
- **`GET /ws/trips/:id?token=`**: WS channel, JWT-authed via query param, closes 4401 if unauthorized/non-party.
- **`POST /trips/:id/start`** (driver, `scheduled→driver_approaching`), **`POST /trips/:id/passenger-aboard`** (driver, `→active`), **`POST /trips/:id/location`** (driver, real ETA via `RoutingProvider`, throttled recompute, proximity auto-transitions to `pickup`/`arriving`), **`POST /trips/:id/tracking-issue`**, **`GET /trips/:id/tracking`** (read model / polling fallback).
- New pure functions: `deriveTrackingStatus`, `computeAutoTripStatusTransition` (`packages/domain/src/trip/`).
- Full doc: `docs/domain/live-tracking.md`.
- **Verified end-to-end against real Postgres/Redis/OSRM** — see `apps/api/src/modules/trips/__tests__/trips-tracking.integration.test.ts` (12 tests) and a manual WS smoke test during implementation (driver start → WS snapshot → location ping → real ETA computed → WS push → proximity auto-transition → unauthorized-socket rejection, all confirmed live).

### Admin platform + driver verification workflow — backend
- **Schema**: `admin_users`, `audit_logs`, `analytics_events`, `reports` tables; `driver_profiles` gained verification-review columns + extended `verification_status` enum (`under_review`, `resubmission_required`); `users` gained account-suspension columns.
- **Admin auth**: `POST /admin/login`, separate JWT payload shape (`type:'admin'`), `authenticateAdmin` hook, cross-rejected against the consumer `authenticate` hook in both directions.
- **Admin module** (`apps/api/src/modules/admin/`): users (list/detail/suspend/reactivate/restrict-driver/unrestrict-driver), rides (list/detail/cancel-with-booking-cascade), verification (queue/detail/approve/decline), reports (list/update), analytics (overview/corridors/search-funnel), audit-logs.
- **Verification workflow reverses a previously "locked" auto-approve decision** — `createOnboarding` now sets `pending`, not `approved`; a real review queue exists for the first time. `rides.service.ts` now enforces `verificationStatus==='approved' && !suspendedAt` server-side before allowing ride creation. `POST /drivers/verification/resubmit` added. Full doc: `docs/domain/verification-workflow.md`.
- **Analytics**: `POST /analytics/events` ingestion (corridor-bucketed via `computeCorridorKey`), admin aggregation endpoints. Full doc: `docs/domain/admin-platform.md`.
- **Verified against real Postgres via `app.inject`**: `apps/api/src/modules/admin/__tests__/admin-verification.integration.test.ts` (8 tests: login, cross-token-rejection, queue listing, decline+resubmit+approve+terminal-state-rejection, audit-log writes), `apps/api/src/modules/analytics/__tests__/analytics.integration.test.ts` (4 tests: ingestion, auth rejection, corridor demand, search funnel).

### Regression fixes and test-suite impact
- `rides.service.ts`'s new verification/restriction gate required updating ~11 existing integration test fixtures (bookings/rides/matching/recurring/conversations/trips suites) to explicitly set `verificationStatus: 'approved'` on their directly-inserted driver profiles, since the schema default is `pending` and several of those tests exercise real ride creation through the service/HTTP layer.
- `tests/e2e/tests/search-to-booking.api.test.ts` (the core search→match→book E2E suite) gained a real admin-login + approve step between onboarding and ride creation, since onboarding no longer auto-approves — this makes the E2E suite exercise the actual production flow rather than reintroducing a bypass.
- **Full `apps/api` suite after all fixes: 171/174 passing.** The 3 failures are pre-existing and unrelated (Google OAuth integration tests need real `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET` this sandbox doesn't have — confirmed unrelated by inspecting the failure, not just assumed).
- `packages/domain` full suite: 89/89 passing (16 new tests across `tracking-status`, `tracking-transitions`, `verification-transitions`, `corridor-key`).

### Dev environment notes
- Docker services (Postgres on `:5433`, Redis on `:6379`, OSRM on `:5001`) were already running in this sandbox — all integration testing above is against real infrastructure, not mocked.
- Seeded admin dev credential: `admin@vaya.tn` / `VayaAdmin2026!` (via `apps/api/src/db/seed.ts` — **change this in any real deployment**).
- `apps/api/.env` was created from `.env.example` for local dev (gitignored, not committed).

## Mobile + admin tracks (both DONE, coordinator-reviewed)

### Mobile app (apps/mobile) — DONE

Built for real this time (the prior session's claim of a dispatched, in-progress subagent was false — verified on resume: zero mobile diff beyond commit `6ca4f87`). All of the following is a real, typecheck/lint/test-verified diff in this worktree, left uncommitted for coordinator review.

**Live tracking — passenger side (`bookings/live.tsx`, full rebuild)**: the old `setTimeout(4000)` mock (no real polyline, a fake 46%-filled progress bar, force-navigated regardless of real state) is gone entirely. New `src/features/tracking/useTripTracking.ts` hook: WebSocket-primary (`GET /ws/trips/:id?token=`) with exponential-backoff reconnect (2s→20s cap), REST polling (`GET /trips/:id/tracking`, 5s interval) as the fallback — RTK Query's `pollingInterval` is forced to 0 whenever the socket is actually connected, so the two transports are never both live at once. Recomputes `trackingStatus` locally every 5s against wall-clock time (reusing `@vaya/domain`'s real `deriveTrackingStatus`, not a reimplementation) so a "live" badge can't silently go stale between server pushes. `AppState` background/foreground handling forces a reconnect + one manual refetch on return to foreground. The screen itself: real `MapCanvas` + `MapRoute` (decoded `routePolyline`, never a straight line), a driver marker built on `MarkerAnimated`/`AnimatedRegion` that tweens between position pushes instead of teleporting, honest per-`trackingStatus` states (`starting` → "waiting for driver," `live` → real marker + real ETA from the last location push's `etaSec` — never a fabricated countdown, `stale` → last-known marker frozen + banner, `unavailable` → greyed marker + manual-refresh banner), a one-time `tracking_issue` banner, and the real completion signal: `trackingState.tripStatus === 'completed'` is what now navigates to `bookings/settlement.tsx`, not a timer. `CancellationSheet`/`NoShowReportSheet`/message wiring kept exactly as-is (regression-checked by the existing `cancellation-sheet-screen.test.ts` source-level test, still passing).

**Live tracking — driver side (`driver/rides/[rideId].tsx`, extended, not rebuilt)**: a ride can have N accepted bookings, each with its own independent `trips` row (one driver position, N trip rows) — handled via a new invisible `AcceptedBookingTripBridge` per accepted booking (one `useGetTripByBookingQuery` subscription each, reporting up via callback, avoiding a variable-length hooks-in-a-loop violation) aggregated into one `tripsByBooking` map. Three real actions: a ride-level "Démarrer le trajet" footer button (visible while any accepted trip is `scheduled`, calls `startTrip` on all of them via `Promise.allSettled`, toasts on partial failure), a per-passenger inline "Passager à bord" button on any row whose own trip is `driver_approaching`/`pickup`, and a ride-level "Terminer le trajet" footer button (visible once every accepted trip is `active`/`arriving`, none still `scheduled`/`driver_approaching`/`pickup`). Passenger rows now show the trip's real status label (`En route`/`Sur place`/`À bord`/`Arrivée proche`) once it exists, falling back to the generic "Confirmé" badge beforehand. New `src/features/driver-rides/useDriverLocationBroadcast.ts`: foreground-only `expo-location.watchPositionAsync` (7s / 15m thresholds — a deliberate choice, documented in the hook, roughly matching the live-tracking design doc's 6-10s guidance without hammering the rate limit), broadcasting every fix to **every** currently-trackable trip on the ride (not just one), reporting `tracking-issue` exactly once per denied-permission episode (not per tick), with a retry-permission banner. **Foreground-only is a stated limitation, not a silent gap**: true background location needs `ACCESS_BACKGROUND_LOCATION`/iOS "Always" entitlements and real battery-review tradeoffs neither this session nor this sandbox can responsibly add or verify.

**Backend side-effect wired in the same change (small, coordinator-authored, not mine to re-describe at length — see the "Second-session backend additions" section above)**: `trips.service.ts`'s `startTrip`/`completeTrip` now flip `rides.status` to `in_progress`/`completed` for real. `driver-rides/myRidesHelpers.ts`'s `computeTripPhase` was updated to trust that real signal first, falling back to its prior departure-time heuristic only for the pre-tracking-start window — one new unit test covers the direct-`in_progress`-trusted-early case.

**Real analytics sink**: `services/analytics/analytics.ts`'s `trackEvent` was a dev-only `console.log` no-op (explicitly documented as a placeholder) — now batches in memory (2s idle debounce or 20-event batch, capped at the server's 50/request limit) and flushes via a new `ingestAnalyticsEvents` RTK mutation, dispatched directly against the real Redux store from outside React (`store.dispatch(api.endpoints.ingestAnalyticsEvents.initiate(...))`), best-effort throughout (a failed flush is dropped, never retried, never surfaced to the user — mirrors `notifyBestEffort`'s server-side philosophy). The event-shape mapping itself (`services/analytics/eventMapping.ts`) is a pure, store-free module specifically so it unit-tests without pulling in the real RTK Query/Redux runtime — `analytics.test.ts` covers named-field extraction vs. the `metadata` catch-all, `undefined`-dropping, and the 64-char `eventName` truncation. Every pre-existing `trackEvent(...)` call site in the app (driver-request-response, cancellation, no-show, etc.) now genuinely ingests server-side with zero call-site changes. New `searchId` field on `searchSlice` (generated once per search session, idempotent via `ensureSearchSession`) threads the 8 canonical `SEARCH_FUNNEL_EVENT_NAMES` through their real UI moments: `search_started` (first field tap on `explore.tsx`), `origin_selected`/`destination_selected` (`search/composer.tsx`'s `choose()`), `search_submitted` (the "Rechercher" CTA), `search_results_shown`/`search_no_results` (`results.tsx`, keyed off the real `searchResult` tier/candidate-count), `search_result_selected` (the shared `useOpenDriver()` hook, so both `results.tsx` and `cluster.tsx` get this for free), `search_abandoned` (fires on `results.tsx` unmount if nothing was ever selected, via a ref, not a redundant state field).

**Verification pending/resubmission UX**: `driver/onboarding/confirmation.tsx` was rebuilt to stop assuming synchronous auto-approval (a premise the admin-verification-workflow initiative explicitly reversed) — it now derives its entire state from a live `useGetMyDriverProfileQuery()` read, not a one-time `status` nav param, and renders 4 real states (`approved`/pending-or-under_review/`resubmission_required`/`rejected`) with distinct badge tones (green/amber/red) and honest copy — the old "your ride will publish automatically once verified" claim (never backed by any real mechanism) was corrected to "you'll publish it yourself once verified." New `driver/onboarding/resubmit.tsx`: reachable only for `resubmission_required`, shows the admin's real structured decline reason (mapped via new pure `verificationDeclineCopy.ts`, unit-tested) plus their required free-text message non-punitively, then re-runs the exact same live `CaptureCamera` 3-step flow (license→insurance→selfie, no gallery-upload path — CLAUDE.md's protected differentiator) before calling `resubmitVerification`. `(tabs)/publish.tsx`'s verification-requirement `BottomSheet` (previously a single "Commencer la vérification" CTA that assumed "not verified" only ever meant "never onboarded") now branches on the real status: a `pending`/`under_review` driver sees "Voir le statut" (no dead-end re-onboarding attempt), `resubmission_required` deep-links straight to the new resubmit screen, `rejected` shows the real decline message. Fixed a real, concrete bug found while doing this: `selfie.tsx`'s post-`createOnboarding` retry-on-CONFLICT path assumed a pre-existing profile meant "verification already done" (true only under the old synchronous-approval model) and would have shown a false "vous êtes vérifié" — now routes to the confirmation screen without asserting a status it can't back, letting it derive the truth itself.

**Document security, mobile side of the coordinator's backend fix**: `selfie.tsx`'s 3 KYC uploads (license/insurance/selfie — the only call site, all three happen at final submit) were repointed from the public `uploadFile`/`POST /uploads` to the new `uploadSecureFile`/`POST /uploads/secure`. Found and fixed one real bug this surfaced: `state/api.ts`'s `resolveMediaUrls` (which turns a relative upload path into an absolute URL for every RTK Query response) only matched `/uploads/`, not `/secure-uploads/` — without the fix, every KYC document submission would have failed `createOnboarding`'s `fileUrl: z.string().url()` validation outright. Caught by typecheck-driven reasoning about the schema contract, not by a failing test (no integration test exercises this exact mobile→API path in this sandbox), so treat this specific fix as logic-verified, not live-request-verified.

**Notification wiring — the 6 new event types** (`trip_arriving`, `trip_tracking_unavailable`, `verification_submitted`, `verification_approved`, `verification_declined`, `verification_resubmission_required`, confirmed via `git diff` against `@vaya/domain` as genuinely new, not guessed): all wired into `deepLink.ts` (the two tracking events resolve to "Mes trajets" — their payload only carries `tripId`/`bookingId`, not everything `bookings/live.tsx`'s params expect, an honestly-documented "good enough, not perfect" tradeoff matching this file's own existing precedent; `verification_resubmission_required` is the one that deep-links directly to the new resubmit screen, matching the file's existing `booking_requested`-style "go straight to the actionable screen" convention) and `notificationCopy.ts` (title/icon/tone/description for all 6, French copy kept consistent with the wording the backend's own `notifications.service.ts` already chose). Full fr/en/ar i18n coverage added for every new string across `notifications.json`, `activeTrip.json`, and `driver.json` (verified valid JSON via `node -e "JSON.parse(...)"` for every touched locale file, not just the one primarily edited in).

**Deliberately NOT built / stated limitations**:
- True background GPS tracking (see above) — foreground-only, by design, given this sandbox's inability to verify native background-permission behavior or real battery impact.
- No E2E/on-device verification of any of this — this project's own long-established precedent ("Known gaps," CLAUDE.md) is that typecheck/lint/unit-test is the bar in this sandboxed environment; nothing here substitutes for actually running the Expo app on a device or simulator, and that has not happened.
- `trip_arriving`/`trip_tracking_unavailable` notification taps land on the generic "Mes trajets" tab, not directly on the live-tracking screen (their payload doesn't carry what that screen's params expect) — a real, documented "good enough" tradeoff, not a full deep-link.
- The mobile→API round-trip for the new `/uploads/secure` + `resolveMediaUrls` fix is logic-verified (typecheck, schema-contract reasoning) but not exercised against a live server from this session — no integration/E2E test in this sandbox covers the mobile onboarding-submission HTTP path at all (it never did, even before this change).

**Verified, not assumed**: `pnpm --filter @vaya/mobile typecheck` clean, `pnpm --filter @vaya/mobile lint` clean (0 errors — only pre-existing warnings unrelated to this change, confirmed by diffing the warning list against a clean baseline run before starting), `pnpm --filter @vaya/mobile test` — **241/241 passing** (up from the pre-existing 232/232 baseline; every new/changed screen either has a real snapshot regenerated and diff-reviewed for exactly the expected structural change — `driver-ride-hub-screen`, `search-results-screen`, `profile-screen`, `trips-screen` — or new unit tests for the pure logic added: `eventMapping.test.ts`, `verificationDeclineCopy.test.ts`, `myRidesHelpers.test.ts`'s new `in_progress`-trust case, `deepLink.test.ts`'s 6 new cases). `packages/domain` was not touched by this pass (confirmed via `git status`) — every new pure helper lives in `apps/mobile`, reusing `@vaya/domain`'s existing `deriveTrackingStatus`/`TripStatus` rather than duplicating it. One real, subtle test-isolation bug found and fixed along the way: `analytics.ts` now imports the real Redux store, which broke `driver-ride-hub-screen.snapshot.test.tsx`'s partial `vi.doMock('../state/api', ...)` (the mocked module lacked `reducerPath`/`reducer`, crashing `configureStore` internally) — fixed by mocking `analytics.ts` directly in that test file, matching the existing precedent already established in `search-trust-screen.snapshot.test.tsx`, plus extracting the pure event-mapping logic into its own store-free module (`eventMapping.ts`) so it can be unit-tested without the same problem.

### Admin web app (apps/admin) — DONE

Built from scratch (previously a literal stub — `"dev": "echo 'Admin dashboard - not yet implemented'"`, zero source beyond `package.json`). A prior session's progress notes claimed this was "dispatched to a subagent, in progress" — that was false; verified directly (`apps/admin/src` had exactly `.gitkeep` and a placeholder `index.ts`) before starting.

**Stack**: Vite + React 19 + TypeScript, React Router 7, TanStack Query 5 for data-fetching/caching (not RTK Query — that's tied to the mobile app's Redux store; a standalone SPA doesn't need a second Redux store just to reuse it). Styling is plain CSS with custom properties (`src/theme/global.css`) rather than a component-library dependency — deliberately, to avoid the "generic AI-admin-template" look CLAUDE.md warns against. `packages/design-system` is **not** imported (it's a React Native package — `react-native-maps` etc. — that would break a Vite web bundle); instead `src/theme/tokens.ts` hand-copies the exact VAYA palette (navy/sage/warm-cream, `packages/design-system/src/tokens/colors.ts`) as the single source of admin-app color, kept in sync manually.

**Screens** (all with real loading-skeleton / empty-state / error-state handling, not defaults):
- **Dashboard**: overview stat tiles (users/rides/marketplace, every ratio rendered as `—` not a fabricated `0%` when its denominator is zero), a prominent **unmet-demand callout** surfacing the top-3 underserved corridors (`isUnderservedCorridor`: demand ≥5 searches and supply covering less than a third of it — a documented UI heuristic, not a server-computed value), a verification-queue glance, and a recent-audit-log glance.
- **Users**: searchable/paginated list, detail (profile, driver/rider profile, last-20 rides/bookings), suspend/reactivate (account-level, blocks all API access) and restrict/unrestrict-driver (narrower — blocks only ride creation) actions, each behind a confirmation modal with a required reason field where the API requires one.
- **Rides**: searchable/paginated list with status filter, detail (itinerary, driver, vehicle, bookings with rider identity + trip status), cancel action with required reason + a cascade-effect warning naming exactly how many active bookings will be cancelled.
- **Driver Verifications** (the flagship screen): queue defaulting to the actionable `pending`+`under_review` view with tabs for the full history, detail screen showing real document images via the new secure endpoint (see below), vehicle info, and a real review-history timeline from `history`/audit logs. Approve/decline flow enforces the schema's real constraints in the UI itself (decline requires both a structured reason AND a non-empty human-readable message — the submit button is disabled and shows an inline error until a message is typed, not just a server-side 400).
- **Analytics**: corridor demand/supply table (the literal "Tunis → Sousse, high search volume, low supply" requirement — underserved rows get a ⚠️ marker and a highlighted left border) plus a search-funnel bar visualization, with a 7/30/90-day window selector.
- **Reports**: list with status filter, detail modal with category/description and a status+resolution-notes update action.
- **Audit Log**: full table, rows expand to show `previousState`/`newState` JSON diffs.

**A real security gap found and fixed while building this** (backend change, not admin-only): verification documents (`verification_documents.fileUrl`) were being served from the API's fully **public** `/uploads/` static directory — reachable by anyone who ever learned the URL, forever, with no auth at all, despite every endpoint that *returned* that URL being correctly gated behind `authenticateAdmin`/ownership checks. Fixed with a new `StorageAdapter.saveSecure`/`readSecure` pair (`apps/api/src/lib/storage/local-disk-storage-adapter.ts`) writing to a `secure-uploads/` directory that is deliberately never registered under `@fastify/static`, plus two new authenticated, streaming-only (never a bookmarkable URL) endpoints: `GET /admin/verifications/documents/:id/file` (admin-only) and `GET /drivers/me/documents/:id/file` (owning driver only — a mismatched driver gets a 404, not a 403, so the endpoint never confirms a document id exists to a non-owner). `POST /uploads/secure` is the new upload endpoint driver KYC capture flows must use instead of the old public `POST /uploads` (avatar/vehicle photos are unaffected — those are meant to be publicly viewable). `readSecure` falls back to the legacy public directory for documents submitted before this fix, so nothing already-approved 404s. Full detail: `docs/domain/verification-workflow.md`'s "Document security" section (updated in the same change). The admin app's `SecureDocumentImage` component consumes this correctly: `fetch` with a Bearer header → `Blob` → `URL.createObjectURL`, since a plain `<img src>` can't carry an Authorization header.

**A second real bug found and fixed via live verification** (see below): `RideDetailPage`'s cancellable-status set originally included `draft` — but `packages/domain/src/ride/ride-status.ts`'s state machine only allows `draft → published`, never `draft → cancelled`. Caught by actually calling the real endpoint against a real draft ride (see verification below), not by inspection; the button would otherwise have been shown and always 409'd. Fixed to `published | full | in_progress`, then re-verified against a real published ride, which cancelled successfully.

**Verified against a real, live backend — not just typecheck/build**: Postgres/Redis were already running in this worktree; the API dev server was already running (started by concurrent work in the same shared worktree) with the demo dataset seeded (43 users, 130 rides across every status, 30 approved + 2 pending + 1 `resubmission_required` driver profiles). Seeded additional throwaway dev data directly via SQL to exercise every screen for real: 2 safety reports, and a realistic demand/supply analytics scenario matching CLAUDE.md's own example almost exactly — corridor `Tunisia → Sousse` with 22 search events against only 1 real published ride (correctly flagged underserved by the dashboard/analytics UI), contrasted with `Tunis → Nabeul`'s 12 searches against 10 rides (correctly *not* flagged). Also wrote a real 160-byte JPEG into `secure-uploads/` and repointed a document row's `fileUrl` at it to prove the secure-image pipeline end-to-end. Every mutation the UI wires up was exercised for real via direct HTTP calls against the live server with a real admin JWT: login, verification approve, verification decline (resubmission_required outcome), user suspend, user reactivate, report status update, ride cancel (both the draft-ride 409 that exposed the bug above, and a published-ride cancel that succeeded) — all confirmed with real before/after response bodies, not assumed from reading the code.

**Real limitation, stated plainly**: Chrome browser automation (`claude-in-chrome`) was unavailable in this sandboxed environment (extension not connected) despite being requested — the same class of limitation this project already documents for the mobile app ("no on-device verification possible"), now also true here for interactive browser click-through specifically in this sandbox, though a Vite web app is in principle fully browser-testable. What *was* verified for real: every HTTP contract the UI depends on, against the live server, with real response bodies inspected — not just "the code compiles." No visual/interaction bug (a misaligned modal, a broken click target, a CSS layout issue) can be ruled out by this method; only contract-shape and business-logic bugs can, and one real bug of exactly that second kind was found and fixed (the draft-ride cancel button).

**Tests**: `pnpm --filter @vaya/admin typecheck / lint / build / test` all clean (11/11 unit tests — `formatRatio`'s null-vs-zero honesty, `isUnderservedCorridor`'s threshold logic, `humanize`). Production build succeeds (`vite build`, ~92KB gzipped JS).

**Deferred, stated honestly**: no E2E/component-interaction tests (React Testing Library is installed but unused beyond the two pure-logic unit-test files) — given the live-HTTP verification above already caught the one real logic bug, this was judged lower-value than it would be without that verification having happened, but it's a real gap, not a false "fully tested" claim. No dark mode (this app doesn't share the mobile app's theme system by design). No mobile-responsive layout — this is an internal ops tool, always used on a desktop browser, matching this pass's own scope call.

## Architecture reference

Full design docs: `docs/domain/live-tracking.md`, `docs/domain/verification-workflow.md`, `docs/domain/admin-platform.md`. `docs/domain/model.md`'s source-of-truth table and state-machine list were updated in the same change.

## Known limitations (backend, stated plainly — see each design doc's own section for full detail)

- No proactive "tracking gone silent" push (client-reported only, not server-polled).
- Corridor supply approximation under-counts route-passthrough-only supply.
- Verification decline reason/message is per-submission, not per-document.
- "Active users" in the analytics overview is an analytics-activity proxy, not a rigorous session-based DAU.
- No background GPS tracking on the driver side — confirmed as a deliberate, permanent-for-now scope decision (foreground-only), not a pending item: true background tracking needs native permission entitlements and battery-impact verification this sandbox cannot responsibly provide. See the "Mobile app" section above.
- Admin web app has not been visually verified in a browser in this sandboxed environment (Chrome automation was unavailable) — typecheck/build/lint/unit-tests plus live HTTP-contract verification against the real running API are the verification bar here; see the "Admin web app" section above for exactly what that did and didn't cover.
- Mobile: `trip_arriving`/`trip_tracking_unavailable` notification taps land on the generic "Mes trajets" tab, not the live-tracking screen directly (their payload doesn't carry what that screen needs) — documented, not silently missing.

## Coordinator review (this session, after both forks reported back)

Both forks' diffs were read directly (not trusted from self-report alone) and one real bug was found and fixed beyond what either fork caught:

- **`useTripTracking.ts`'s REST-fallback staleness bug**: `rawState = wsState ?? restQuery.data` meant that once the WebSocket connected even once, `wsState` (never reset back to `undefined`) permanently took priority over `restQuery.data` — so if the socket later dropped for good, the screen would keep showing the stale WS snapshot forever instead of the actively-polling REST fallback's fresher data, defeating the entire point of having a fallback. Fixed to `connectionState === 'live' ? wsState : (restQuery.data ?? wsState)` — trust the socket only while it's actually connected; prefer the polling data the instant it isn't. Re-typechecked clean after the fix.
- Spot-checked `useDriverLocationBroadcast.ts`, `bookings/live.tsx`, `driver/rides/[rideId].tsx`'s journey-control diff, `state/api.ts`'s `resolveMediaUrls`/secure-upload fix, and the admin app's `SecureDocumentImage.tsx` + decline-verification required-message validation directly against source — all correct, no further issues found.
- Confirmed zero file-level conflicts between the two forks (`git diff` on every shared backend file — `storage-adapter.ts`, `local-disk-storage-adapter.ts`, `admin.routes.ts`, `admin-verification.service.ts`, `drivers.routes.ts`, `drivers.service.ts`, `uploads.routes.ts`, `trips.service.ts` — showed exactly the coordinator's own pre-dispatch diff, untouched/re-duplicated by either fork).
- Ran the full monorepo suite from the repo root after both forks reported: `pnpm typecheck` (8/8 packages clean), `pnpm lint` (0 errors anywhere, only pre-existing warnings), `pnpm test` per-package — `@vaya/api` 194/198 (4 pre-existing/environmental: missing Google OAuth creds, missing OSRM Tunisia extract in this sandbox), `@vaya/mobile` 241/241, `@vaya/domain` 89/89, `@vaya/design-system` 117/117, `@vaya/admin` 11/11, `@vaya/validation` 6/6, `@vaya/config` 3/4 (1 pre-existing failure, `DEFAULT_LOCALE` hardcoded to `'en'` against a test expecting `'fr'` — confirmed via `git diff`/`git log` that this file was untouched by anything in this entire initiative, a genuinely unrelated pre-existing bug, left alone per this project's own anti-scope-creep principle). `tests/e2e`'s API project: 4 passed, 4 skipped (OSRM-dependent stop-candidate cases, same environmental gap), 0 failed — the core search→match→book loop (now gated behind real admin verification) still works end-to-end.
- `pnpm install` confirmed the lockfile (grew by ~850 lines for the admin app's new dependencies) is fully in sync, no manual drift.

## Exact next steps if resuming from this document

Both tracks are done and coordinator-verified. Only remaining work: commit in logical chunks and push the branch (never merge to `main`, never force-push) — see the top-level chat report for the final commit summary.
